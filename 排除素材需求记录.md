# 排除素材需求记录（2026-08-15，✅ 已实现 v1 + 独立 Tab 重构）

> v1 实现完成（2026-08-15）：图标素材（点击器）排除全链路 + 场景判定
> marker 级排除运行时判定。
> **排除示教最终形态（2026-08-15 用户反馈后重构）**：从画面示教页
> 拆出独立 Tab「排除示教」（ExclusionTeachWidget），流程 =
> 📷 截图观察整屏 → 🔍 截图对比自动定位图标（低阈值预筛 + 升序候选）
> → ⏭ 下一处遍历（抠出候选显示，可红框圈排除特征 + 蓝框遮罩标注）
> → 💾 保存排除素材（多次保存自动追加到图标条目 exclusions）。
> 画面示教页（TeachConsole）中不再包含任何排除示教内容。
> 场景素材的排除示教入口待后续细化（运行时判定已支持 marker.exclusions）。

## 一、需求来源与目标

画面中存在多个相似图标（模板匹配精度有限，全部能被识别为可点击图标），
但图标上有不同的状态标记（如红点/蓝点）。需要区分：
- **红点图标**：不应点击（被排除）
- **蓝点图标**：不应点击（被排除）
- **无点图标**：可以点击

目标：给操作识别素材（图标）附加「排除素材」，运行时按排除特征逐个筛选
图标实例，只对**未被排除**的实例执行点击/判定。

## 二、现有机制（需求基础）

1. **点击器**：模板匹配 → `cv2.minMaxLoc` 取**全局最高分**位置 →
   遮罩范围内随机点点击。多个相似图标时无脑点最高分，无法区分状态。
   （用户原以为点"第一个"，实际是点最高分；同分时取扫描顺序最早 = 通常左上）
2. **场景判定**：只判断"是否存在"（任一标识匹配即命中），不区分状态。
3. **图标计数**：`_match_all_templates` NMS 去重统计全部实例（含坐标+分数），
   已经能拿到所有候选实例——排除筛选可在此基础上实现。
4. **匹配技术**：模板带 alpha（示教遮罩产物）→ 彩色匹配；无 alpha → 灰度。
   搜索区域 = 素材条目 region（红框）。

## 三、排除素材设计草案（初稿）

### 数据结构
操作识别素材条目 json 增加可选字段：
```json
"exclusions": [
  {"image": "red_dot.png", "region": [相对该图标的区域], "threshold": 0.85}
]
```
- 排除素材 = 独立的小图标条目（同款遮罩 PNG + 相对搜索区域）
- **仅对该图标素材生效**（绑定关系存图标条目内）
- 一个图标可绑定多个排除特征（红点、蓝点……），**任一命中即排除**

### 运行时判定流程
1. 匹配主图标（同现有逻辑）
2. 得到候选实例（单实例取最高分；多实例走 NMS 全列表）
3. 对每个候选实例：
   - 在其图标范围内（主图标匹配位置 + 排除素材的相对区域）搜索排除特征
   - 命中任一排除特征 → 该实例**跳过**
   - 全部不命中 → 该实例**通过**
4. 点击器：取第一个通过的实例（按分数/位置顺序）点击；全被排除 → not_found
5. 场景判定：标识组命中主图标但被排除 → 该标识不通过
6. 图标计数：可选输出"通过数/总数"（待定）

### UI 构思（用户初步想法，自认为有问题，待重新设计）
用户原想法：画面示教旁加「素材示教」，打开场景/操作/OCR 素材，
按蓝框数显示遮罩图标整体，在整体上红框选固定位置、蓝框确定标识组、
遮罩涂排除特征 → 保存后素材多一个或多个排除素材。

**用户已确认此 UI 想法有问题，正在重新思考解法。**

## 三、示教采样困境（用户 2026-08-15 补充，核心逻辑）

用户原 UI 构想（在素材整体上示教排除特征）存在**根本性采样矛盾**：

1. **单次截图只能呈现一种状态**。一个素材往往有多种互斥状态（无点/红点/蓝点…），
   一次截屏只能截到其中一种：
   - 若截到的是「应该通过」的状态 → 没法添加排除素材（没有任何排除特征可标）
   - 若截到的是「不通过」的状态（如红点）→ 只能教会「红点=排除」，
     但不知道「蓝点=排除」也是需要的
2. **同一位置的状态互斥**。素材的同一位置（如左上角）可能有红点**或**蓝点，
   两种状态不可能同时出现——任何单次示教都无法一次性获得全部排除情况。
3. **结论**：排除特征天然是「多次采样累积」的产物，单次示教模型无法覆盖。
   需要一个能**多次追加**排除特征的机制（每次示教添加一个新的排除特征，
   而非一次性定义完整集合）。

> 用户继续思考解法中。待解法确定后再进入实现。

### 解决方向提示（供参考，未定案）
- **多次追加式示教**：同一图标素材可反复进入"排除示教"，每次把当前截屏的
  状态差异标为一个新排除特征（红点一次、蓝点一次），追加进 exclusions 列表
- **按位置分组**：排除特征登记为"相对位置 + 特征模板"对，多个模板共享同一
  相对搜索区域；判定时区域内任一模板命中即排除
- **循环演示采集**：示教时多次截图（用户手动切换游戏状态），每次标注差异
- 判定侧不变：命中任一排除特征 → 排除该实例

### 「截图对比」方案（用户 2026-08-15 新想法，待分析确认）

素材示教新增「截图对比」按钮 + 「下一处」按钮，用于定位"需要排除"的图标：

1. 发现需要排除的状态时点「截图对比」：按点击器逻辑（红框区域找蓝框特征
   整体）截图，把识别到的图标按遮罩抠出显示在图片显示区
2. **低精确度预筛**：对比时降低匹配阈值。若低阈值都无法识别到 → 正常高
   阈值运行时必然不会匹配通过 → 该图标无需排除；低阈值识别到了 → 进入
   后续处理（定位/标注排除特征）
3. **多候选遍历**：可匹配图标中**优先匹配度最低的**（刚过阈值的），
   「下一处」向匹配度更高的方向逐个跳转——先看最可疑（状态差异导致分数
   略低）的实例；识别到的是"可通过"图标则点「下一处」跳过，直到找到
   需要排除的图标
4. 找到目标图标后，按之前的红框+蓝框+遮罩方式标注排除特征（多次追加）

> 可行性分析（2026-08-15 初步）：整体可行。基础：_match_all_templates 已
> 返回全部 (x,y,w,h,score) 实例，排序/筛选/抠图均为现成能力。
> 自洽要点：候选集合应限定 score ≥ 正常阈值（0.85）的实例——低于正常阈值
> 的实例运行时根本不会匹配通过，无需排除；"低阈值预筛"负责判断要不要
> 进入流程，最终遍历仍应在 ≥0.85 集合内按分数升序。这样背景误匹配
> （低分噪音 <0.85）天然被排除在候选之外，不干扰遍历。

## 四、待讨论/待定问题

1. 排除特征（红点/蓝点）是从**图标素材本身的遮罩图**上裁剪子区域，
   还是单独截图示教？——排除特征相对图标的位置如何稳定（图标本身会匹配
   到不同屏幕位置，相对坐标应可靠）
2. 排除素材的搜索范围：图标匹配框内？还是图标附近扩展区域？
3. 多个候选实例的遍历顺序：分数降序？从上到下？
4. 点击器加排除后，旧任务（无 exclusions 字段）行为不变 ✓
5. 场景判定的排除粒度：标识组（蓝框）级 or 整个场景级
6. UI 编辑入口：在「详情」类弹窗加排除素材列？还是示教式？
7. 排除素材与 OCR 素材的关系：OCR 是否也需要排除？（用户未提，暂不做）

## 五、定案结论（2026-08-15，全部落地）

1. 排除特征：红框圈相对区域（相对图标框）+ 蓝框遮罩标注特征 → 裁剪 RGBA
   存 `excl_{ts}.png`（与图标条目同目录，ASCII 文件名），`region` 存红框
   相对坐标，`threshold` 默认 0.92。
2. 搜索范围：图标匹配框内按相对区域换算（`area=[mx+int(rx*mw), ...]`）。
3. 遍历顺序：候选按匹配分数**升序**（最可疑的差异实例最先看），「⏭ 下一处」
   向匹配度更高方向跳转。
4. 旧任务无 exclusions 字段 → 行为不变 ✓（有回归测试）。
5. 场景判定排除粒度：标识组（蓝框 marker）级，`_match_marker_score` 主块
   命中后检查 marker.exclusions；场景素材排除示教入口待后续。
6. UI 编辑入口 = **独立 Tab「排除示教」**（visual_builder_panel 第四页），
   不与画面示教混用。
7. OCR 素材排除暂不做。

