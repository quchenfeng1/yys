# 2026-07-19 工作日志

## 说明书 v1.7 修订（根据附录D用户确认事项）

用户确认了附录D待确认事项第1-7项和第9项（第8、12项之前已确认），据此全面修订说明书 v1.6 → v1.7：

### 新增内容
1. **第十二部分 账号管理与多开体系**（全新章节）
   - 账号体系：1主号 + 最多2小号
   - 多区切换：主号可配多区服，小号固定区服
   - 多开控制：每账号绑定独立模拟器实例（独立ADB端口）
   - 小号任务范围：仅常驻副本(permanent)，不执行日常/活动/特殊
   - 主号小号协作：独立刷取 + 组队协作模式
   - accounts.yaml 配置格式
   - 与其他模块的集成关系

2. **5.10 账号管理与多开模块**（core/account_manager.py）
   - AccountManager 核心接口 + Account 数据结构
   - switch_account 设备切换、get_task_scope 任务范围过滤

3. **11.4 小号设置一级菜单**（与脚本配置、任务控制同级别）
   - 小号独立模拟器端口、副本配置、阵容

### 修改内容
- 1.2 核心功能清单：新增 F12 账号管理与多开
- 2.2 运行环境：默认MuMu但兼容其他模拟器
- 3.1 分层架构图：核心服务层新增"账号管理"
- 第四部分目录结构：新增 config/accounts.yaml、core/account_manager.py
- 5.8 阵容管理：新增"阵容检查的强制性"（首次进战前必须检查、锁定与否在副本配置）
- 6.4 常驻任务流程：添加阵容检查强制性引用
- 7.2 配置文件：新增 accounts.yaml 引用说明；修复 global.yaml 包名为 wyzymnqsd_cps、adb路径为MuMu自带
- 11.1 菜单总览：三大块（配置/任务/小号设置），新增账号管理子菜单
- 11.2 脚本配置：任务优先级精确到每个任务、运行时段改为全天运行
- 附录D：第1-9项+第12项标记已确认，保留第10/11/13/14/15项待确认

### 用户确认要点
1. 模拟器：默认MuMu，其他也可
2. 多区切换：新增账号管理模块
3. 账号最多3个(1主+2小)，小号只配合刷副本，新增小号设置一级菜单
4. 日常任务：先加现有，以后补充
5. 优先级：配置菜单下，精确到每个任务
6. 运行时段：脚本全天，任务靠时间规则控制
7. 时间规则：所有任务单独设置
9. 阵容切换：首次进战前必须检查，锁定与否在副本配置

## 说明书拆分为13个子文件

将原 `脚本模块说明书.md`（2156行、118KB）按目录拆分为13个独立子说明书，放入 `说明书/` 文件夹：
- 01-项目总览.md ~ 13-附录.md
- 每个子文件含统一头部（标题+版本说明+完整说明书指引）+ 对应部分内容
- 原说明书完整保留未修改
- 子文件间交叉引用已改为相对路径链接（如 [05-核心模块详解](05-核心模块详解.md)）

## 模块拆解方案设计

用户提出8模块解耦思路（定时/任务/传参/UI/配置/识图控制/安全/连接），要求分析可行性并输出每个模块的目的作用实现思路到"模块拆解"文件夹。

### 分析结论
- 整体思路**优秀可行**，核心亮点：任务=特化+通用组合、模块间先后/判断关系、传参模块作UI桥梁、安全模块独立
- 改进建议：新增3个模块（状态管理/事件总线/运行控制）、拆分1个模块（传参拆为配置模型+UI绑定）
- 最终方案：**11个模块**（核心运行层5+支撑服务层4+桥梁表现层2）

### 输出文件（14个，共3068行）
- `模块解耦思路.md`（总览，含可行性分析+改进建议+6个疑问+模块依赖图）
- `01-连接模块.md` ~ `11-UI模块.md`（11个模块详细文件，含目的/作用/接口设计/实现思路/交互关系）
- `主程序设计思路.md`（启动流程+依赖注入装配+数据流+目录结构+v0.5迁移路径）
- `UI设计思路.md`（三栏布局+子模块设计+可复用组件+UI与传参协作流程）

### 关键设计决策
1. 任务模块升级为"任务步骤图(DAG)"，支持顺序/条件分支/循环/错误恢复
2. 引入事件总线实现模块间发布-订阅解耦通信
3. 引入状态管理集中全局运行态（账号/连接/场景/运行状态）
4. 从定时模块拆出运行控制模块（管启停生命周期，定时模块只管时间调度）
5. 安全模块扩展覆盖全操作链路（点击/滑动/等待/节奏/上限）
6. 传参模块内部拆分配置数据模型层+UI数据绑定层

### 向用户提出的6个疑问（待确认）
1. 任务步骤图(DAG) vs 简单线性先后关系
2. 事件总线间接通信 vs 直接调用
3. 运行控制独立成模块
4. 安全模块覆盖全链路 vs 仅识图
5. 通用模块颗粒度（粗/中/细）
6. 传参模块内部拆分

## 模块拆解 v2.0（用户确认+进一步优化）

用户确认全部6项优化建议（DAG/事件总线/运行控制独立/安全全链路/中粒度/传参拆分），并要求进一步优化。

### v2.0变更
1. **新增第12个模块：日志与监控模块**（Monitor）
   - 结构化日志（模块/级别/任务/步骤/时间戳/标签）
   - 运行指标收集（执行次数/成功率/平均耗时）
   - 异常自动截图（出错截图+上下文JSON）
   - 运行报告（每日/每周Markdown报告）
   - 日志事件化（通过事件总线发布，UI订阅）
2. **任务模块增强**：步骤超时/重试/回滚cleanup/中断检查点/并行执行
3. **事件总线增强**：事件溯源(回放)/事件优先级(系统级插队)/事件去重(防风暴)/死信队列
4. **配置模块增强**：三级分层覆盖(global→account→task)/配置版本迁移/配置导入导出
5. **安全模块增强**：操作审计日志/行为指纹多样性(长时间运行渐变)/操作节奏控制/卡住检测
6. **运行控制增强**：异常恢复策略(连接/识图/超时/未知)/熔断机制(连续失败5次停止)/任务依赖感知
7. **连接模块增强**：连接质量监控(延迟统计)/连接池预热(多开)/健康度评分
8. **识图模块增强**：多尺度匹配/识图结果缓存/性能监控
9. **定时模块增强**：调度可视化/任务冲突检测/最小堆优化
10. **状态管理增强**：状态历史记录/状态校验/快照恢复
11. **传参模块增强**：参数变更回滚/批量编辑/参数预设

### 最终文件（15个，共4702行）
- `模块解耦思路.md`（v2.0总览，状态改为已确认，新增第十节进一步优化方案）
- `01-连接模块.md` ~ `12-日志与监控模块.md`（12个模块，各含"六、进一步增强"段落）
- `主程序设计思路.md`（v2.0，含Monitor装配+熔断器+异常恢复+迁移路径更新）
- `UI设计思路.md`（v2.0，新增运行监控面板+日志导出+模块筛选）
