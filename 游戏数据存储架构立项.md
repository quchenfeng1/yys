# 游戏数据存储架构立项书（数据库式 game_id 关联）

> 立项日期：2026-08-16 ｜ 状态：**立项（未实施）**
> 背景：用户提出「类似数据库的存储：各种素材、任务都关联游戏的 id，
> 切换游戏时就是换一个游戏 id 去库里找素材」，认为比"切换即重建组件"更优。

---

## 一、现状（B方案，2026-08-16 已实施）

- 每个游戏一个目录：`games/{game_id}/`（profile.yaml / tasks/ / assets/ / scenes/ /
  nodes/ / visual_tasks/ / runtime/ / tasks.yaml / coords/）
- 切换游戏 = `bootstrap.switch_game(game_id)` **整体重建**以下组件：
  配置中心游戏路径、调度器（状态存储）、任务注册表、任务文件管理、
  ImageManager/OCR/Recognizer、VisualTaskStore、SceneStore、CompoundStore、
  TeachEngine、VisualBridge、RunController（进度路径/信号/场景库）
- 收益：已达成"各游戏独立世界"（任务/队列/进度按 game_id 隔离），
  且每个游戏的数据物理隔离、可整体备份/删除
- 成本：切换时 ~8 个组件重建（实测 offscreen 下约 2 秒内，可接受）；
  组件持有旧引用时必须逐一换新（遗漏 = 串游戏 bug 源）

## 二、目标架构（数据库式）

核心思想不变：**一切业务数据都带 game_id 键**；变的是存储与访问方式：

```
统一存储层（SQLite：meta.db）
├── tasks(game_id, name, definition_json, ...)
├── scenes(game_id, id, name, signal, judgements, ...)
├── icons(game_id, task, entry, image_path, region, ...)
├── compounds(game_id, name, subgraph, ...)
└── runtime(game_id, task, state_json, ...)

重资产文件（图片/截图）仍放 games/{game_id}/assets/ 等目录，
数据库只存元数据与路径（避免 SQLite 存 blob 的膨胀）。
```

切换游戏 = 组件**常驻**，仅把当前查询键从 game_id=A 换成 game_id=B：
- O(1) 切换、无需重建任何组件
- 天然支持跨游戏查询（统计、素材复用、任务模板迁移）
- 一致性由事务保证；物理目录可保留作文件层

## 三、两方案对比

| 维度 | A：目录隔离 + 切换重建（现状） | B：数据库化 + game_id 列（目标） |
|---|---|---|
| 切换成本 | 重建 ~8 组件（约秒级，实测可用） | O(1) 换查询键 |
| 实现成本 | 已落地 | 需迁移 4+ 个 store 为统一接口 |
| 串游戏风险 | 重建遗漏 → bug（已靠回归覆盖） | 查询键集中，风险低 |
| 跨游戏查询 | 无 | 天然支持 |
| 备份/迁移 | 目录级拷贝 | 数据库文件 + 目录 |
| 多实例并发 | 目录写冲突（现状即可多窗口） | SQLite 锁（需 WAL） |

## 四、建议演进路径

1. **短期（现状保持）**：目录隔离方案已满足"多游戏切换"，继续使用；
   `switch_game` 重建组件清单固化为回归用例（verify_game_emulator_switch.py 已有 18 项）
2. **中期（P1 索引层）**：引入 `core/game_meta.py`（SQLite WAL）只做**元数据索引**
   （任务/场景/图标条目），四个 store 增加 `game_id` 感知的查询接口，
   文件层不动 → 先拿到"换 key 查询"与跨游戏统计能力
3. **长期（P2 全库化）**：runtime 状态、调度记录入库存；多实例写冲突用 WAL+事务解决；
   为「多窗口各跑各的游戏」提供并发安全底座

## 五、触发条件（何时动手）

- 游戏数量 ≥ 3 且切换频繁（当前 1 个，暂无痛感）
- 需要跨游戏统计/素材迁移功能
- 多窗口并发写同一游戏目录出现冲突

## 六、验收标准（P1）

1. 元数据索引含 game_id 列，任务/场景/图标查询均可按 game_id 过滤
2. 切换游戏不再重建 VisualTaskStore/SceneStore/CompoundStore（引用不变）
3. verify_game_emulator_switch.py 仍全绿；新增跨游戏查询用例
