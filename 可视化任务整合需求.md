# 可视化任务 × 游戏任务整合需求（2026-08-16，用户口述 → 文档化）

> 目标：可视化构建完善后，可视化任务进入游戏任务体系，成为正式可调度任务；
> 执行配置聚焦"如何被推送到任务队列"，变量/常量承担原来散落各处的业务参数。

## 一、现状梳理（实现前确认的事实）

1. **可视化任务存储**：`games/{game}/visual_tasks/{name}.json`（节点图 + 示教产物 +
   `param_values` 变量运行参数）。
2. **可视化任务已注册执行引擎**：bootstrap `_init_task_registry` 把 visual_tasks
   目录全部注册进 TaskRegistry（类带 `_definition`），**调度/执行链路已打通**。
3. **调度器数据源**：只从 `tasks.yaml`（`config.tasks_config`）加载任务配置。
   可视化任务**不在 tasks.yaml** → 调度器不认识它 → 任务列表看不到、无法调度。
4. **游戏任务面板**：左任务列表（TaskManager 扫描 tasks 目录的 TaskMeta）+
   右配置表单（⚙ 执行配置 + ⚔ 战斗配置 双 Tab，`uses_battle` 控制后者显隐）。
5. **变量体系**（已实现）：图内变量组/常量组节点（带组名）→ 面板变量配置 tab
   输入 → 任务 `param_values`；运行时 `${key}` 引用 / loop `count_var` 覆盖固定次数。
6. **冲突确认**：
   - 「📊 循环次数」组（loop_count 输入）：已被变量组/`count_var` 覆盖 → **移除**；
     tasks.yaml 旧字段兼容保留（scheduler 仍读 repeat.loop_count，旧任务不破）。
   - 「⚔ 战斗配置」Tab（御魂/战前准备/战斗参数/体力/组队）：战斗参数属于业务逻辑，
     由变量/常量承载 → **移除 UI**；tasks.yaml 旧字段兼容保留。
   - 「活动循环次数」(total_count，失效机制) /「周期触发次数」(max_daily)：
     调度层语义，与变量组不冲突 → **保留**。

## 二、需求定案

### 1. 可视化任务进入游戏任务「任务列表」
- 可视化任务保存/创建时，自动在 `tasks.yaml` 注册一条**调度配置条目**
  （幂等：已存在不覆盖；新条目默认 category=daily、enabled=True）。
- 保存后发布 `VISUAL_TASK_CHANGED` → 任务列表/任务队列刷新，立即可见。
- 任务列表标记类型 `[visual_task]`。

### 2. 执行配置 = 「任务如何被推送到队列」的调度设置
- 保留分组（按用户要求分块框起）：
  - **⏱ 时间调度（必配）**：重复规则 / 触发信号 / 每周几 / 间隔值 / 每月几号 /
    执行时段 / 周期触发次数 / 活动有效期 / 活动循环次数
  - **其他**：优先级 / 下次执行
- **移除「📊 循环次数」组**（变量组 `count_var` 已覆盖）。
- 表单保存 `loop_count` 不再由 UI 写入（兼容读取旧值）。

### 3. 取消「⚔ 战斗配置」Tab
- 御魂配置 / 战前准备 / 战斗参数 / 体力配置 / 组队配置 UI 全部移除
  （由变量组/常量组承载业务参数）。
- tasks.yaml 既有字段（soul_setup/teaming/lock_team/change_team/team_id/floor/
  stamina_required/max_fail_streak）保留兼容——运行时读取不依赖 UI。

### 4. 新增「🔢 变量配置」「📌 常量展示」Tab（执行配置旁）
- 仅**可视化任务**显示（老任务无变量定义 → 空态提示）。
- 数据源：可视化任务定义（visual_tasks/{name}.json 的 graph 中变量组/常量组节点）。
- **按组名分组显示**（QGroupBox，组名即框题——这就是变量组节点需要「组名」的原因）：
  - 变量配置：每组内变量按类型渲染输入框（int/float/bool/text），
    初值 = 任务 `param_values`（缺省用变量默认值）；保存 → 写回可视化任务 `param_values`。
  - 常量展示：只读蓝字（值来自常量组定义）。
- 冲突提示（跨组重名等）保留。

## 三、实现方案（代码动线）

1. `core/bootstrap._init_visual`：VisualBridge 增传 `config_manager`。
2. `ui/param_bridge/visual_bridge.py`：
   - `save_task`/`create_task` 后 `_ensure_schedule_entry`（tasks.yaml 注册条目，
     缺省 category/enabled/display_name）。
3. `ui/param_bridge/task_bridge.py`：
   - `get_task_metas` 合并 registry 可视化任务（`task_type="visual_task"`、
     `is_visual=True`）。
   - `get_task_detail` 对 visual 任务补 display_name/category（registry 类属性）。
4. `ui/main_window.py`：GameTaskPanel 注入 `visual_bridge`。
5. `ui/panels/game_task_panel.py`：
   - `_render_form`：删循环次数组、删战斗 Tab；visual 任务加两个变量 Tab；
     执行配置保持「时间调度 + 其他」两组框。
   - `_collect_config`/`_save`：不再写 loop_count/战斗字段；新增变量配置
     收集 → `visual_bridge.save_task` 写回 `param_values`。
6. 测试更新：verify_save_chain / verify_schedule_ui / verify_trigger_signal /
   verify_battle_tab / verify_battle_config_chain / verify_sub_multiselect /
   verify_account_manage 等 UI 断言同步；新增 verify_visual_in_game_task.py。

## 四、兼容性承诺（旧数据不破）

- tasks.yaml 旧字段（loop_count / soul_setup / teaming / lock_team / change_team /
  team_id / floor / stamina_required / max_fail_streak）继续被调度器/运行时读取。
- 老任务（无变量定义）表单行为不变（仅少循环次数/战斗配置两个 UI 区）。
- 战斗任务运行时链（combat_test / coop）不动。
