# 阴阳师自动化脚本 — 任务优先级书（v2.3）

> **生成日期**：2026-07-19 ｜ **依据**：项目结构全面调查
> **原则**：依赖优先、核心先行、可跑第一、逐步完善

---

## 当前状态总览

| 层级 | 完成度 | 说明 |
|------|--------|------|
| 核心引擎 | 91% | 缺 `team_manager.py`、`account_manager.py` |
| 设备驱动 | 100% | ADB 连接/截图/点击/心跳全就绪 |
| 任务框架 | 100% | TaskStep + TaskGraph + 9个通用模块 |
| 登录流程 | 100% | `login_flow.py` 完整可用 |
| UI 界面 | 91% | 三栏布局+菜单+控制栏+日志终端+任务队列 |
| 🌐 素材资源 | **100%** | ✅ v2.3 按场景分区（9个），ImageManager 可视化面板就绪 |
| 📋 具体任务 | **0%** | ❌ daily/permanent/event/special 全部空 |

---

## 第一阶段：填充素材（P1 — 已大幅降低）

> **目标**：通过 UI 图片配置面板，逐场景截取并导入素材。
> v2.3 已提供可视化面板，无需手动操作文件系统。

| 序号 | 任务 | 操作方式 |
|------|------|----------|
| 1.1 | 截取主界面素材 | UI → 图片配置 → 主界面 → ＋添加图片 |
| 1.2 | 截取探索素材 | UI → 图片配置 → 探索 → ＋添加图片 |
| 1.3 | 截取战斗素材 | UI → 图片配置 → 战斗 → ＋添加图片 |
| 1.4 | 截取通用按钮 | UI → 图片配置 → 通用 → ＋添加图片 |
| 1.5 | 截取其余分区 | 召唤/商城/阴阳寮/活动/阵容 按需添加 |

**完成后检查点**：脚本启动 → 连接模拟器 → 素材加载 N>0 → 日志无素材缺失警告

---

## 第二阶段：补齐核心模块（P0 — 阻断性）

> **目标**：实现 `team_manager.py` 和 `account_manager.py`，消除运行时崩溃风险。

| 序号 | 任务 | 产出 | 依赖 |
|------|------|------|------|
| 2.1 | 实现 `core/team_manager.py` | 阵容预设管理 + 对局外调整 + 对局内选择 + 锁定 | 第一阶段（需要识图定位阵容界面） |
| 2.2 | 实现 `core/account_manager.py` | 主号/小号管理 + 多区切换 + 设备绑定 | — |
| 2.3 | 更新 `tasks/base/task_context.py` | 注入真实的 TeamManager/AccountManager 而非 `None` | 2.1, 2.2 |

**完成后检查点**：`TaskContext.team_manager` 不再是 None，手动调用 `ensure_team("yuhun_speed")` 不崩溃

---

## 第三阶段：让脚本能"跑"（P1 — 首个可运行任务）

> **目标**：实现第一个可完整运行的日常任务（每日签到），打通全链路。

| 序号 | 任务 | 产出 | 依赖 |
|------|------|------|------|
| 3.1 | 截取签到相关素材 | `assets/tasks/daily/sign_in/` | 第一阶段 |
| 3.2 | 实现 `tasks/daily/sign_in.py` | 每日签到任务（进入→签到→领奖→返回） | 3.1, TaskStep 框架 |
| 3.3 | 截取悬赏封印素材 | `assets/tasks/daily/xuanshang/` | 第一阶段 |
| 3.4 | 实现悬赏封印任务 | 识别悬赏目标→决定挑战/放弃 | 3.3 |
| 3.5 | 集成 TaskGraph 到 `script_worker.py` | UI 启动按钮走 RunController → Scheduler → TaskGraph | 第二阶段 |
| 3.6 | 端到端测试 | 点击启动 → 登录 → 执行签到 → 返回庭院 → 停止 | 全部上述 |

**完成后检查点**：点击 UI「启动」→ 脚本完成登录 → 自动执行签到 → 日志显示"签到成功" → 返回庭院待命

---

## 第四阶段：副本战斗（P1 — 核心玩法）

> **目标**：实现主要副本战斗任务，覆盖御魂、觉醒、结界突破。

| 序号 | 任务 | 产出 | 依赖 |
|------|------|------|------|
| 4.1 | 截取御魂副本素材 | `assets/tasks/permanent/yuhun/` | 第一阶段 |
| 4.2 | 实现 `tasks/permanent/yuhun.py` | 御魂副本：进入→选层→战斗循环→结算 | 4.1, 第三阶段 |
| 4.3 | 截取觉醒副本素材 | `assets/tasks/permanent/juexing/` | 第一阶段 |
| 4.4 | 实现觉醒副本 | 同 4.2 模式，仅入口不同 | 4.3 |
| 4.5 | 实现结界突破 | 个人突破 + 寮突破 | 第三阶段 |
| 4.6 | 实现阵容切换全流程 | TeamManager 全程打通：检测→调整→选阵容→锁定 | 2.1 |
| 4.7 | 多副本连续运行测试 | 签到→御魂×30→觉醒×10→结界突破→停止 | 4.1-4.6 |

**完成后检查点**：脚本连续运行 30 分钟以上，完成多副本切换，无崩溃

---

## 第五阶段：完善与打磨（P2）

> **目标**：补齐剩余任务，完善 UI 面板，提高稳定性。

| 序号 | 任务 | 产出 |
|------|------|------|
| 5.1 | 实现花合战、逢魔之时、地域鬼王 | `tasks/daily/` 下新文件 |
| 5.2 | 实现契灵、业原火、御灵副本 | `tasks/permanent/` 下新文件 |
| 5.3 | 实现真八岐大蛇（多阶段） | `tasks/special/true_orochi.py` |
| 5.4 | 填充 `ui/widgets/` | task_row / repeat_editor / countdown_label / team_editor |
| 5.5 | 填充 `config/coords/teams/` | 各阵容预设 JSON |
| 5.6 | 实现 `ui/panels/` 下剩余面板 | connection/account/priority/anti_detect 等 ~10 个面板 |
| 5.7 | 小号多开联调 | 主号+小号协作刷副本 |
| 5.8 | 文档终审 | 遍历说明书 13 个文件，确保与代码一致 |

---

## 依赖关系图

```
Phase 1 (素材)
  └─→ Phase 2 (核心模块)
        └─→ Phase 3 (首个任务, 全链路)
              └─→ Phase 4 (副本战斗)
                    └─→ Phase 5 (完善打磨)
```

---

## 工作量估算

| 阶段 | 新代码量 | 素材量 | 预计工时 |
|------|----------|--------|----------|
| 第一阶段 | 0 | ~20 张截图 | 1-2 小时 |
| 第二阶段 | ~400 行 | 0 | 3-4 小时 |
| 第三阶段 | ~200 行 | ~10 张 | 2-3 小时 |
| 第四阶段 | ~500 行 | ~30 张 | 4-6 小时 |
| 第五阶段 | ~800 行 | ~50 张 | 6-8 小时 |
| **合计** | **~1900 行** | **~110 张** | **16-23 小时** |
