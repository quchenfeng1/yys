# 阴阳师自动化脚本 — 执行逻辑书（v2.4）

> **目的**：面向非 Python 开发者的完整执行流程说明。
> 读完本文后，你将理解：脚本如何启动、每步做了什么、素材放哪里、
> 模块调用顺序、如何新增任务。

---

## 零、启动前准备

### 你需要准备什么

1. **模拟器**（推荐 MuMu 12，分辨率设为 1280×720）
2. **模拟器中已安装阴阳师**
3. **双击 `启动.bat`**（不要用命令行）

### 启动.bat 做了什么

```
双击 启动.bat
  → 尝试用 pythonw.exe 运行 main.py（无黑窗）
  → pythonw.exe 找不到就用 python.exe 运行
```

---

## 一、程序启动（main.py → 显示窗口）

```
启动.bat
  └→ main.py 开始执行
       │
       ├─[1] 全局禁止子进程弹黑窗
       │      install_global_patch() → 所有 subprocess 调用不弹 CMD
       │
       ├─[2] 初始化 PyQt5 应用
       │      QApplication → 创建 GUI 框架
       │
       ├─[3] 创建主窗口 MainWindow
       │      ├─ 左栏：菜单树（MenuTree）
       │      ├─ 中栏：控制栏(ControlBar) + 任务队列(TaskQueuePanel) + 任务列表
       │      └─ 右栏：日志+终端面板(LogPanel)
       │
       └─[4] 进入事件循环 app.exec_()
              等待用户操作（点击按钮、菜单等）
```

**此时你看到的**：一个三栏窗口——左侧菜单、中间控制按钮、右侧日志终端。

---

## 二、点击「启动」按钮后的完整流程

### 总览（8 步流水线）

```
用户点击 [▶ 启动]
  → ControlBar 发出信号
  → MainWindow._on_start()
  → 创建 ScriptWorker（后台线程）
  → 执行 8 步流水线
```

### Step 1：加载配置

```
ScriptWorker._execute()
  └→ ConfigManager() 创建
       ├─ 读取 config/global.yaml     （全局配置：ADB端口、分辨率、防封参数）
       ├─ 读取 config/tasks.yaml      （任务配置：哪些任务启用、优先级、执行规则）
       └─ 读取 config/accounts.yaml   （账号配置：主号/小号信息）
```

**关键文件**：`config/global.yaml` — 所有全局参数在这里改

### Step 2：连接模拟器

```
ScriptWorker
  └→ EmulatorManager.ensure_running()
       ├─ 检测模拟器进程是否在运行
       ├─ 没运行 → 自动启动模拟器 exe
       ├─ 等待模拟器启动完毕（最多 90 秒）
       └→ ADBClient.connect()
            ├─ 执行 adb connect 127.0.0.1:{端口}
            ├─ 验证连接：adb get-state
            └─ 连接成功 → 继续
               连接失败 → 报错停止
```

**你改端口**：`config/global.yaml` → `adb.port: 16384`

### Step 3：读取分辨率（只读，不修改）

ScriptWorker
```
  └→ ADBClient.get_screen_size()
       └─ 执行 adb shell wm size
            ├─ 分辨率为 1280x720 → OK 继续
            └─ 分辨率不匹配 → 警告，但不修改（需要你在模拟器设置里手动改）
```

### Step 4：启动阴阳师 App

```
ScriptWorker
  └→ ADBClient.launch_app()
       └─ 执行 adb shell am start -n {包名}/{Activity}
            └─ 阴阳师启动 → 等待加载
```

### Step 5：加载素材图片

```
ScriptWorker._load_images()
  └─ 扫描「所需图片/」文件夹
       └─ 对每个 .png 文件：
            ├─ 查 IMAGE_MAP 表 → 知道它属于哪个目录
            ├─ 复制到 assets/{对应目录}/{文件名}
            └─ 例：courtyard_main.png → assets/scenes/courtyard/main.png
```

**素材放在哪**：把模拟器截图放入 `所需图片/` 文件夹，脚本启动时会自动归类到 `assets/`。

**IMAGE_MAP 映射表**（在 `ui/script_worker.py` 第 68-84 行）：

| 你的截图文件名 | 自动归类到 |
|--------------|-----------|
| `courtyard_main.png` | `assets/scenes/courtyard/` |
| `challenge_btn.png` | `assets/common/battle/` |
| `close_btn.png` | `assets/common/ui/` |
| `splash_skip.png` | `assets/scenes/login/` |
| ... | ... |

### Step 6：初始化核心模块（识图 + 防封 + 执行）

```
ScriptWorker
  ├─ Recognizer() 创建
  │    └─ 扫描 assets/ 全部目录，加载所有 .png 到内存
  │        ├─ common/  → 公共按钮（战斗/UI/导航）
  │        ├─ scenes/  → 场景标志图（庭院/登录/探索）
  │        ├─ tasks/   → 任务专属图
  │        └─ teams/   → 阵容预设图
  │
  ├─ AntiDetect() 创建
  │    └─ 读取 global.yaml 的防封参数
  │        ├─ 点击偏移半径
  │        ├─ 延迟抖动系数
  │        └─ 走神概率
  │
  └─ Executor() 创建
       └─ 绑定 ADBClient + Recognizer + AntiDetect
           以后所有操作都通过 Executor，强制经过防封号
```

### Step 7：检查素材

```
ScriptWorker._check_images()
  └─ 检查是否有必需的素材缺失
       ├─ 有缺失 → 报错停止，提示缺哪些
       └─ 齐全 → 继续
```

### Step 8：执行登录流程

```
ScriptWorker
  └→ LoginFlow.execute()
       ├─ 检查是否已在游戏中（识别庭院标志图）
       │    ├─ 已在 → 跳过登录
       │    └─ 不在 → 开始登录
       │
       └─ _process_login_steps()
            ├─[a] 等待开屏动画出现 → 点击跳过
            ├─[b] 等待公告弹窗 → 点击关闭（循环直到无弹窗）
            ├─[c] 等待选区界面 → 识别目标区服 → 点击
            ├─[d] 等待"进入游戏"按钮 → OCR 识别位置 → 循环点击
            └─[e] 等待庭院主界面出现 → 登录成功！
```

---

## 三、核心模块如何协作（单次点击的完整链路）

这是整个脚本最重要的一段逻辑。当你看到一个任务代码写 `executor.click_image("某个按钮")` 时，实际发生了：

```
任务代码：executor.click_image("common/battle/challenge_btn")
  │
  ├─[1] Recognizer 识别
  │     └─ ADBClient.screenshot()           ← 截取当前模拟器画面
  │     └─ cv2.matchTemplate(截图, 模板)     ← OpenCV 找按钮位置
  │     └─ 返回: MatchResult(center=(637, 598), confidence=0.92)
  │
  ├─[2] AntiDetect 防封号
  │     └─ random_offset(637, 598)           ← 正态分布随机偏移
  │     └─ 返回: (641, 602)                  ← 不点正中心，模拟人手不准
  │     └─ human_move_path(当前位置, (641,602)) ← 贝塞尔曲线轨迹
  │     └─ maybe_long_pause()               ← 5% 概率触发走神暂停
  │
  ├─[3] ADBClient 执行
  │     └─ adb shell input tap 641 602      ← 通过 ADB 发送点击
  │
  └─[4] 记录与等待
        └─ record_action()                   ← 累计操作次数
        └─ random_sleep(0.8~1.3秒)           ← 随机延迟
```

---

## 四、素材体系详解

### 四层目录 → 场景分区（v2.3）

v2.3 将素材从"按任务类型"改为**按游戏场景分区**，与游戏本身的界面逻辑一致：

```
assets/
├── 主界面/      ← 庭院标志、探索入口、签到入口、町中入口
├── 探索/        ← 御魂入口、觉醒入口、秘闻入口、章节选择
├── 召唤/        ← 召唤按钮、十连、跳过动画
├── 商城/        ← 商城入口、购买按钮、每日免费礼包
├── 战斗/        ← 挑战按钮、胜利标志、失败标志、准备按钮
├── 阴阳寮/      ← 寮入口、结界突破、道馆、麒麟
├── 活动/        ← 限时活动 Banner、活动入口、活动兑换
├── 通用/        ← 跨场景通用按钮（关闭、确认、返回）
├── 阵容/        ← 阵容验证图、选阵容分步图
└── .image_meta.json  ← 图片元数据（备注、尺寸、引用关系）
```

**优势**：多个任务共用一张图。例如御魂和觉醒都在「探索」分区，共用一个"探索入口"图片。

### 代码中如何引用素材

```python
# 按"分区/文件名"引用（不含 .png）
executor.click_image("战斗/挑战按钮")      # → assets/战斗/挑战按钮.png
executor.click_image("主界面/探索入口")     # → assets/主界面/探索入口.png
recognizer.exists("通用/关闭按钮")          # → assets/通用/关闭按钮.png
```

### 如何添加新素材（可视化操作）

1. 点击左侧菜单「🖼 图片配置」→ 选择目标分区（如「战斗」）
2. 点击「＋ 添加图片」→ 选择你的截图 PNG
3. 选中图片 → 点击「✎ 编辑备注」→ 写上用途说明
4. 双击图片可预览真实尺寸

### 代码中如何引用素材

不需要写路径，只写 `目录/文件名`（不含 `.png`）：

```python
# 正确写法
executor.click_image("common/battle/challenge_btn")   # → assets/common/battle/challenge_btn.png
executor.click_image("scenes/courtyard/main")          # → assets/scenes/courtyard/main.png
recognizer.exists("tasks/permanent/yuhun/entry")       # → assets/tasks/permanent/yuhun/entry.png
```

### 如何添加新素材

1. 在模拟器中截图（1280×720）
2. 裁剪出按钮/图标的最小区域
3. 保存为 `.png`，放入 `所需图片/` 文件夹
4. 在 `script_worker.py` 的 `IMAGE_MAP` 中添加一行：
   ```python
   "你的文件名.png": "assets下的目标子目录",
   ```
5. 重启脚本 → 自动归类

---

## 五、任务模块详解（如何新增一个任务）

### 现有两套体系

| 体系 | 文件 | 用途 |
|------|------|------|
| **旧体系** | `tasks/base_task.py` → `BaseTask` | 登录流程在用，简单直接 |
| **新体系** | `tasks/base/task_step.py` → `TaskStep` + `TaskGraph` | v2 推荐，支持条件分支/循环 |

### 方式一：用旧体系新增任务（最简单）

以每日签到来举例：

**第一步**：截图素材，放入 `assets/tasks/daily/sign_in/`：
- `sign_btn.png` — 签到按钮
- `claim_btn.png` — 领取按钮

**第二步**：创建 `tasks/daily/sign_in.py`：

```python
"""每日签到任务"""
from tasks.base_task import BaseTask

class SignInTask(BaseTask):
    name = "sign_in"
    display_name = "每日签到"

    def run(self) -> bool:
        # 1. 点击签到入口
        self.executor.click_image("tasks/daily/sign_in/sign_btn", timeout=10)
        
        # 2. 点击领取
        self.executor.click_image("tasks/daily/sign_in/claim_btn", timeout=5)
        
        # 3. 关闭弹窗
        self.executor.click_if_exists("common/ui/close_btn")
        
        return True
```

**第三步**：在 `tasks/daily/__init__.py` 中注册（或让它被自动扫描发现）。

**就这三步**。`pre_check()` 和 `post_check()` 不用写，基类有默认实现。

### 方式二：用新体系新增副本任务（推荐，支持循环）

以御魂副本为例：

```python
"""御魂副本任务"""
from tasks.base.task_graph import TaskGraph
from tasks.base.task_step import TaskStep, StepResult
from tasks.base.task_context import TaskContext

# 第一步：定义特化步骤（每个副本独有的操作）
class EnterYuhun(TaskStep):
    """进入御魂副本"""
    name = "enter_yuhun"
    timeout = 30

    def execute(self, ctx: TaskContext) -> StepResult:
        # 从庭院 → 探索 → 御魂入口
        ctx.executor.click_image("scenes/courtyard/icon_explore", timeout=5)
        ctx.executor.click_image("tasks/permanent/yuhun/entry", timeout=5)
        return StepResult.success("已进入御魂界面")

class SelectFloor(TaskStep):
    """选择层数"""
    name = "select_floor"
    
    def execute(self, ctx: TaskContext) -> StepResult:
        # TODO: 根据配置选择层数
        ctx.executor.click_image("tasks/permanent/yuhun/floor_select", timeout=5)
        return StepResult.success("已选层数")

# 第二步：组合步骤图
class YuhunTask:
    name = "yuhun"

    def build_graph(self) -> TaskGraph:
        g = TaskGraph(task_name="yuhun")
        
        # 特化步骤
        g.add_step("enter", EnterYuhun())
        g.add_step("floor", SelectFloor())
        
        # 通用步骤（从 tasks/common/ 导入，全项目复用）
        from tasks.common.pre_check_team import PreCheckTeam
        from tasks.common.select_team import SelectTeam
        from tasks.common.battle_loop import BattleLoop
        from tasks.common.claim_reward import ClaimReward
        from tasks.common.return_hall import ReturnHall
        from tasks.common.error_return import ErrorReturn
        
        g.add_step("team", PreCheckTeam())
        g.add_step("select", SelectTeam())
        g.add_step("battle", BattleLoop(times=30))  # 刷30次
        g.add_step("claim", ClaimReward())
        g.add_step("return", ReturnHall())
        g.add_step("error", ErrorReturn())
        
        # 连接步骤：enter → floor → team → select → battle → claim → return
        g.set_entry("enter")
        g.add_edge("enter", "floor")
        g.add_edge("floor", "team")
        g.add_edge("team", "select")
        g.add_edge("select", "battle")
        g.add_edge("battle", "claim")
        g.add_edge("claim", "return")
        
        # 任意步骤失败 → 走 error 分支
        for s in ["enter", "floor", "team", "select", "battle", "claim"]:
            g.add_edge(s, "error", condition=lambda r: r.status == "fail")
        
        return g

    def execute(self) -> bool:
        ctx = TaskContext(task_name="yuhun")
        return self.build_graph().run(ctx)
```

### 通用模块清单（全项目复用，不需要重写）

| 模块 | 文件 | 作用 |
|------|------|------|
| `BattleLoop` | `tasks/common/battle_loop.py` | 等待战斗结束→检测胜负→结算，可循环 N 次 |
| `ClaimReward` | `tasks/common/claim_reward.py` | 领奖 + 关弹窗 |
| `ReturnHall` | `tasks/common/return_hall.py` | 反复点返回直到回到庭院 |
| `PreCheckTeam` | `tasks/common/pre_check_team.py` | 检查阵容是否匹配 |
| `SelectTeam` | `tasks/common/select_team.py` | 副本内选阵容+锁定 |
| `ErrorReturn` | `tasks/common/error_return.py` | 失败时返回大厅+跳过任务 |
| `ClosePopup` | `tasks/common/close_popup.py` | 关闭所有弹窗 |
| `CheckStamina` | `tasks/common/check_stamina.py` | 体力检查 |

---

## 六、配置文件速查

### global.yaml 关键配置

```yaml
adb:
  port: 16384           # ← 改这个换模拟器端口
  device_id: "127.0.0.1:16384"

screen:
  width: 1280           # ← 模拟器分辨率（不要改）
  height: 720

anti_detect:
  click_offset_radius: 12   # 点击偏移（越大越安全但越慢）
  max_daily_runtime: 8      # 每天最多跑几小时
  max_daily_actions: 2000   # 每天最多点击几次
```

### tasks.yaml 关键配置

```yaml
daily:
  - name: "sign_in"
    enabled: true            # ← false = 不执行这个任务
    priority: 1              # ← 数字越小越优先
    repeat:
      type: "daily"          # 每天执行
      at_time: "00:00"       # 0点后可执行
      times: 1               # 执行1次

permanent:
  - name: "yuhun"
    enabled: true
    priority: 10
    repeat:
      type: "weekly"         # 每周执行
      weekdays: [1, 3, 5]   # 周一、三、五
      at_time: "14:00"       # 下午2点
      times: 30              # 刷30次
```

---

## 七、目录与文件对应速查

```
你想改什么                     → 去哪个文件
─────────────────────────────────────────────────────
ADB 端口、模拟器设置            → config/global.yaml
哪些任务启用、优先级             → config/tasks.yaml
任务具体怎么执行                → tasks/daily/xxx.py 或 tasks/permanent/xxx.py
添加新按钮截图                  → UI 图片配置面板 → 选分区 → ＋添加图片
管理图片（预览/备注/删除）        → 左侧菜单「🖼 图片配置」→ 选分区
防封号强度                      → config/global.yaml → anti_detect 段
UI 布局                         → ui/main_window.py
UI 左侧菜单                     → ui/panels/menu_tree.py
任务队列面板                    → ui/panels/task_queue_panel.py
启动/停止按钮                   → ui/panels/control_bar.py
日志终端面板                    → ui/panels/log_panel.py
登录流程                        → tasks/login_flow.py
识图引擎                        → core/recognizer.py
任务管理器（扫描/创建/删除）        → core/task_manager.py ★v2.4
防封号引擎                      → core/anti_detect.py
操作执行器                      → core/executor.py
任务步骤图引擎                  → tasks/base/task_graph.py
通用战斗循环                    → tasks/common/battle_loop.py
事件总线                        → core/event_bus.py
```

---

## 八、事件系统速查

模块之间通过"事件"通信，不直接互相调用。重要事件：

| 事件名 | 谁发出 | 谁接收 | 含义 |
|--------|--------|--------|------|
| `start_requested` | UI 启动按钮 | RunController | 用户点了启动 |
| `stop_requested` | UI 停止按钮 | RunController | 用户点了停止 |
| `task_started` | RunController | UI + Scheduler | 某个任务开始执行 |
| `task_done` | RunController | UI + Scheduler | 某个任务执行完毕 |
| `step_done` | TaskGraph | UI + Monitor | 某个步骤完成 |
| `schedule_updated` | Scheduler | TaskQueuePanel | 任务队列刷新 |
| `state_changed` | StateManager | UI StatusBar | 全局状态变化 |
| `connection_lost` | Connection | RunController + UI | 连接断开 |

---

## 九、一句话总结

```
启动.bat → main.py → 显示 UI 窗口
点击「启动」→ ScriptWorker 线程
  → 加载配置 → 连模拟器 → 启动阴阳师 → 加载素材
  → 初始化识图/防封/执行器 → 执行登录流程
  → 登录成功后，Scheduler 按日程表逐个执行任务
  → 每个任务 = 若干步骤（特化 + 通用）的有向图
  → 每个步骤 = 识图定位 → 防封偏移 → ADB 点击
```
