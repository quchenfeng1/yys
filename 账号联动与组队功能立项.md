# 账号联动与组队功能立项书

> 立项日期：2026-08-16 ｜ 状态：**立项（未实施）**
> 背景：2026-08-16 完成「模拟器管理 + 游戏切换 B方案」后，用户提出：
> ① 小号管理更名账号管理（**已实施**），账号需与模拟器联动
> ② 组队功能（大号+小号跨设备协作）尚未想好实现方式，先立项

---

## 一、现状盘点（2026-08-16）

| 项 | 现状 |
|---|---|
| 账号配置 | `config/accounts.yaml`：`account_id / name / role(main\|sub) / device_id / enabled / region / remark` |
| 账号面板 | `ui/panels/sub_account_panel.py`，已更名「账号管理」；`AccountBridge`（ui/param_bridge/account_bridge.py） |
| device_id 字段 | **已有但未消费**：无任何代码按 device_id 自动绑定/切换账号 |
| 模拟器体系 | 新增 `core/emulator_store.py` + `config/emulators.yaml` + 顶部模拟器下拉（切换即连接，serial=host:port） |
| 组队残留 | `games/yys/tasks/common/coop_host / coop_join / coop_passive`（跨设备组队协调占位）；`TEAMING_PREPARED / COORDINATE_ACTION` 事件已定义未消费 |
| 队伍配置 | 老程序有 `teams/` 目录与阵容体系；新程序 `config/coords/teams/`、游戏目录 `teams/` 为空 |

## 二、目标

### 目标 1：账号 ↔ 模拟器联动（小）
- 模拟器条目增加与账号的关联（复用 accounts.yaml 的 `device_id` 即可，无需新表）
- 顶部模拟器下拉切换成功后：
  - 查找 `device_id == serial` 的账号 → 自动切换当前账号（AccountManager.current_account / ACCOUNT_SWITCHED 事件）
  - 状态栏显示当前账号名
- 账号管理面板：`device_id` 列改为「绑定模拟器」下拉（从 EmulatorStore 选，而非手填地址）
- 未绑定任何账号的模拟器 → 保持当前账号不变，仅提示

### 目标 2：信号触发与任务联动（用户细化方案 v4，2026-08-16）

**不新建菜单**。在可视化构建基础节点中新增一组节点：

| 节点 | 作用域 | 语义 |
|---|---|---|
| 场景判断节点（现有，改兜底行为） | 任务内 | false/miss 未接线时**先走任务内置场景识别器**（不是直接进全局任务） |
| **场景信号接收节点**（新） | 任务内 | 任务内置场景识别器命中本任务场景 → 发场景信号 → 本节点接收并触发后续 |
| **任务信号输出节点**（新） | 任务内/全局任务内 | 发任务信号（如「更换阵容」）；全局任务内也可发 |
| **任务信号接收节点**（新） | 任务内 | **仅任务正在执行时生效**，收到任务信号（含跨窗口）后继续向下 |
| **跨窗口任务信号输出/接收**（新） | 多窗口 | 经信号邮箱跨窗口收发任务信号 |
| **任务信号触发器**（新） | 单窗口 | 图内存在此节点 = 该任务是触发任务，设立对应信号名；后接调度器分支 |
| **跨窗口任务信号触发器**（新） | 多窗口 | 同上，跨窗口 |
| **调度器分支节点**（scheduler_ops，新） | 单窗口 | 4 个出口：加入待执行队列 / 加入正在执行队列 / 跳过周期 / 任务失效 |
| **等待节点**（wait，新） | 任务内 | 暂停等待信号传入；可设置等待时间 |
| **超时节点**（timeout，新） | 任务内 | **仅输出**；等待超过设定时间 → 走超时节点后面的逻辑 |

#### 2.1 执行语义：正在执行队列可多任务、同刻只跑一个

- 「正在执行」列表可同时出现**多个任务**，但同一时间只执行一个
- 多个任务同时在列表 = 互有关联，或触发的任务优先级更高 → **暂停当前任务**，执行触发任务
- 任务**暂停期间**：待执行任务**不会**推入正在执行；等正在运行的任务超时或结束
- 等待节点 = 暂停当前任务、等待信号传入；超时 → 走超时节点分支

#### 2.2 触发任务新体系（取代「特殊条件触发」，旧场景信号全部退役）

- **旧场景信号全部退役**：没有「场景信号触发任务」了，只有**任务信号触发的任务**
- 退役范围：SCENE_SIGNAL 对外发布、repeat.type=trigger + trigger_templates 截图触发体系、
  game_task_panel 的「特殊条件触发」下拉与触发信号多选（代码保留兼容，功能下线）
- 触发任务判定：**图内是否使用了「任务信号触发器 / 跨窗口任务信号触发器」**
- 任务信号可由：任务内「任务信号输出节点」或「全局任务内」的任务信号输出节点发出
- 任务信号 → 触发**所有**设立了对应信号名的触发任务 → 走各自后面的调度器分支：
  加入待执行队列 / 加入正在执行队列
- **任务信号接收节点（含跨窗口）只在任务正在执行时生效**，用于和其他任务联动
- 含信号触发节点的任务到期后**不进待执行队列**，进**待触发队列**
- 队列面板新增「📡 待触发」独立列表（不再混入已失效区）
- 待触发任务仍需时间配置：
  - 下次执行时间到了才被推入待触发区；不在执行时段无法被触发
  - 触发后与其他任务一样计次数：每天 ×3 → 触发 3 次后进**未开始区**，无法再触发
  - 每隔 n 小时 → 触发任务结束后进未开始区，下次执行时间 = n 小时后
- 任务信号触发器后面接**调度器分支**：
  - 加入待执行队列 → 之后就跟待执行任务一样正常触发
  - 加入正在执行队列 → 可与正在执行队列的其他任务联动
  - 跳过周期 / 任务失效

#### 2.3 调度器新增功能（任务信号触发器节点调用）

| 功能 | 语义 |
|---|---|
| 加入待执行队列 | 推入待执行区（效果 = 下次执行时间已到） |
| 加入正在执行队列 | 推入正在执行区；上一任务暂停或结束时自动执行（最高优先级待执行；可在任务暂停时执行） |
| 跳过周期 | 本轮跳过（计数+1），按规则推到下一周期 |
| 任务失效 | 置为失效，不再触发 |

#### 2.4 可行性（对照现有架构）

| 技术点 | 现状 | 结论 |
|---|---|---|
| 图内跳转/入口 | graph_runner 已有 entry_id + loop 回跳先例 | ✅ 小改 |
| 等待/超时 | GraphContext 已有可中断 sleep；等待节点=暂停+等信号，超时分支=NodeResult.goto 新出口 | ✅ 小改 |
| 暂停/恢复 | RunController 已有 pause/resume；需加「任务级暂停（让位）」与恢复编排 | ✅ 中改 |
| 五区队列 | 四区（正在执行/待执行/未开始/已失效）已有，加「待触发」区 | ✅ 小改 |
| 触发任务判定 | 图内扫描任务信号触发器/跨窗口任务信号触发器节点（vstore 定义加载时即可判定） | ✅ 小改 |
| 信号→任务匹配 | 单窗口 EventBus 事件 + 信号名注册；跨窗口用信号邮箱 | ✅ 复用组队方案 |
| 防封 | 同线程串行执行 → 间隔/偏移自然累加 | ✅ 无影响 |

#### 2.5 全局任务 Tab（任务上层兜底处理，v3 新增，v4 扩展）

- 可视化构建中新增**「全局任务」Tab**：定义一张兜底图，接收：
  ① 所有任务中**未处理的信号**；② **运行异常**（如点击器点击后页面没有跳转，
  多次未跳转=异常，任务本会报错挂掉 → 统一改为触发全局任务）
- **全局任务本身也是一个任务**：同样内置场景识别器，识别**该游戏的全部场景素材**，
  可统一处理各任务的未处理信号/异常
- 信号流（v4）：
  - 场景判断节点 false 未接线 → **先走任务内置场景识别器**（不是直接进全局任务）
    - 识别到该任务的场景 → 对任务发出**场景信号** → 任务内场景信号接收节点接收 → 触发后续
    - 连续由同一个场景判断节点没识别到，且场景识别器发现都是同一场景 → 异常 → 上抛信号触发全局任务
  - 任务信号（任务内或全局任务内输出节点发出）→ 触发设立对应信号名的触发任务
- 语义 = **在每个任务上层扣了一张相同的任务**：任务出现未处理信号/异常 → 进入全局任务，
  从开始节点走一系列流程 → 结束节点 → 原任务**安全结束**（不挂掉）
- 用途：集中处理特异信号、页面未跳转等异常的重试/止损策略，避免每个任务图重复画

#### 2.6 已确认设计决策（v5，2026-08-16）

1. **场景识别器范围**：非全局任务 → 只识别**本任务素材库的场景**；全局任务 → 识别**该游戏全部场景素材**
2. **异常判定公式**（场景判断/页面未跳转统一用）：
   - 触发任务场景识别器的**场景判断器 id** 与识别器判断出的**场景信号**连续 **n 次相同** → 异常
   - 判断器 id 变化 或 场景信号变化 → **计数重置**
   - 连续 **m 秒**内连续识别到同一场景信号 n 次 → 也为异常
   - 次数阈值 n 与时间窗口 m 放在**设置 UI** 中可调
3. **场景信号接收匹配**：每个任务的场景信号接收节点所设信号**必须互不相同**（一个场景信号对应一个处理，不会同时触发多节点）；内部匹配键用**场景 id**（唯一安全），信号名（中文）仅作展示，可另行为信号名对应唯一标识
4. **未接线出口兑底**：场景判断节点的 false/miss/not_found 等**所有未接线出口**都先走任务场景识别器，识别器处理不了才上抛全局任务（不要直接进全局任务）
5. **点击后页面未跳转**：同一判定公式（页面未变化 = 场景信号连续相同 → 异常）
6. **异常处理**：异常**直接终止该任务流程**，由全局任务接管并**安全结束**；进入全局任务时记录异常（任务名/时间/异常原因/异常位置）→ UI 新增**「异常一览」**界面；走场景识别器时是正常逻辑不计异常
7. **计数语义**：安全结束后的循环计数/每日次数**无需特殊处理**（变量每次重设后保存，逻辑通顺即可）

#### 2.6.1 已确认设计决策（v6 增补，2026-08-16）

8. **场景判断器 id = 任务节点稳定 id**：重复判定键 =（节点 id, 场景信号）；同一节点连续触发场景识别器且识别出相同信号 → 重复计数累加（红框连续 3 次走过同一判断器且信号相同 → 异常）
9. **判定触发时机**：任务级场景识别器统一负责；**所有节点触发场景识别器时都做重复判断**（不只是场景判断节点）
10. **未接线出口**：全部走场景识别器，不设间隔；识别出的场景无对应接收节点 → 上抛全局任务
11. **n/m 参数**：设置 UI 新建「异常检测」组（默认值待定）
12. **异常一览界面**：新→旧排序表格（任务名/时间/异常原因/异常位置/场景信号）+「处理」按钮跳转到对应任务的对应节点；先做界面原型
13. **全局任务发任务信号** = 把触发任务的下次执行时间设为当前时间的**到期触发逻辑**
14. **全局任务上下文**：不需要向全局任务传原任务参数
15. **全局任务自身异常**：先不处理防递归（遇到再解决）
16. **全局任务保存**：作为一个特殊的、独一无二的任务保存；**不会被任务信号触发**
17. **正在执行队列语义**：加入正在执行队列的任务处于**暂停态**，不会暂停正在运行的任务；等正在运行的任务结束或暂停 + 收到任务信号才触发；异常时等待结束后报错结束
18. **任务信号匹配**：精确匹配；预留参数（payload）能力
19. **同信号多任务**：按优先级进待执行排队，或进正在执行队列（暂停态）；都不会立即执行，获取信号后才执行
20. **暂停任务恢复**：信号触发，否则一直暂停
21. **触发计数**：每天×3 复用现有实现
22. **待触发区样式**：与其他队列一致，按老版本
23. **跳过周期** = 下次执行时间设为下个周期开始，进**未开始队列**（不进失效队列）
24. **等待/超时**：暂停节点在画布上设置暂停时间；超时 → 进入全局任务安全退出
25. **任务信号发送后行为（重要）**：信号输出节点发送后**不暂停当前任务**，继续向下走到暂停节点或结束节点；单窗口：其余正在执行任务全部处于暂停态（已结束的排除）→ 按优先级（相同随机）触发一个收到该信号的暂停任务；跨窗口：等对端窗口任务结束或暂停后，触发收到信号的任务
26. **延后事项**：跨窗口任务信号（本需求完成后实施）；旧场景信号退役与「特殊条件触发」删除（本部分完成后再做，届时删重复规则并更新测试）

#### 2.6.2 已确认设计决策（v7 增补，2026-08-16）

27. **场景识别器调用时机**：所有**未连线的输出端口**都调用任务场景识别器（点击器/拖拽器识别不到走 false 天然覆盖；截图器仅一个输出点不会未连线，无需特判）
28. **n/m 默认值**：n=5 次，m=30 秒（设置 UI「异常检测」组可调）
29. **异常「处理」按钮**：跳转到可视化构建的「流程编排」Tab，打开异常任务并定位到异常节点
30. **信号管理面板**：新增「信号管理」，三个 Tab——场景信号 / 触发信号 / 任务信号；原场景素材附带的 signal 迁移到这里集中管理
31. **触发任务的图入口**：信号触发后，任务信号触发器节点被激活 → 走其后调度器分支：加入待执行队列 → 排队按顺序从 start 正常执行；加入正在执行队列 → 任务进入暂停态；调度器 out 接暂停节点
32. **触发计数**：全局任务发信号触发的任务同样计入每天×3——执行成功后次数+1；达每天上限 → 下次执行日期设为下个周期开始
33. **异常任务机制**：「异常一览」更名「**异常任务**」；异常任务**不进入任何队列**直到确认修复；界面：任务列表带「已修复」按钮，点任务看异常履历（最新在上，带「处理」按钮，点击后变「已处理」）；全部处理完才能点「已修复」，否则弹窗「异常未处理」
34. **信号触发去重**：只有**待触发队列**中的任务会被信号触发；已触发任务脱离队列，不会重复触发
35. **暂停任务唤醒顺序**：仅**收到信号**的任务才能脱离暂停态；没收到信号即使队列只剩它也不执行；顺序 = 收到信号 → 优先级高 → 随机
36. **跨窗口任务信号**：与单窗口同逻辑——收到信号的任务加入正在执行队伍（暂停态），等前面的任务结束或暂停才能轮到
37. **退役里程碑**：旧场景信号退役 + 删除「特殊条件触发」重复规则，待本需求完成后实施

#### 2.7 待完善清单

**✅ 全部待完善点已在 v5/v6/v7 确认完毕，设计收敛，可进入实施。**

实施顺序建议（P2 拆解）：
1. 信号管理面板（场景信号/触发信号/任务信号三 Tab + 旧 signal 迁移）
2. 节点库新增：任务信号输出/接收、场景信号接收、任务信号触发器、调度器分支、暂停节点（可设时间）、超时节点
3. 场景识别器改造：未接线出口全部走任务场景识别器 + 异常判定引擎（节点 id+信号连续 n 次/m 秒，默认 5 次/30 秒）
4. 全局任务：特殊唯一任务保存、异常接管与安全结束、异常记录
5. 异常任务面板：履历/处理/已修复确认
6. 调度器：待触发区（五区队列）、加入待执行/正在执行队列、暂停态唤醒（信号→优先级→随机）、跳过周期、任务失效
7. 退役：旧场景信号体系 + 删除「特殊条件触发」+ 更新受影响测试（verify_trigger / verify_trigger_scene_signal）

## 三、阶段划分

| 阶段 | 内容 | 依赖 |
|---|---|---|
| P0（已做） | 小号管理更名账号管理；模拟器管理 + 游戏切换 B方案 | — |
| P1 | 账号↔模拟器绑定：device_id 下拉、切换联动、状态栏 | EmulatorStore（已完成） |
| P2 | **任务内信号体系（本需求主体）**：按 2.7 实施顺序 1-6 落地 | 设计已收敛（v7） |
| P3 | **跨窗口任务信号**：信号邮箱通道（复用组队方案 d:\yys\组队联动方案.md） | P2 |
| P4 | 退役：旧场景信号 + 删除「特殊条件触发」+ 测试更新 | P2 验收后 |
| P5 | 真机联调（大号带小号）：体力不足→换阵容→继续 完整链路 | P2-P4 + 素材示教 |

## 四、验收标准

**P1**：见上文目标 1。
**P2**：
1. verify_signal_nodes.py：等待节点触发 → 任务暂停显示等待 → 触发任务（含信号触发节点）经调度器分支推入正在执行 → 执行完发信号 → 原任务恢复（offscreen 全链路）
2. 队列面板五区：待触发区独立显示；触发任务到期进待触发、不进待执行
3. 每天×3：触发 3 次后进未开始区；每隔 n 小时：结束后进未开始区且 next_run=n 小时后
4. 暂停期间待执行任务不推入正在执行；超时节点按设定时间走超时分支
5. 调度器四功能：加入待执行/加入正在执行/跳过周期/任务失效 各自生效
**P3**：
6. 双进程测试：窗口1 跨窗口信号 → 窗口2 触发任务执行 → 回复 → 窗口1 恢复
7. 断线容错：对端窗口关闭 → 本端按超时策略降级
