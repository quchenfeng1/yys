# 节点审查与外部参数方案（2026-08-15）

## 一、节点使用场景审查（先不删，仅记录）

用户认定的基础节点：开始 / 结束 / 信号触发器 / 场景判定 / 截图器 / OCR读取器 / 点击器 / 拖拽器 / 等待 / 循环。

对其余节点的审查结论：

| 节点 | 结论 | 理由与替代方案 |
|------|------|----------------|
| **branch 分支** | ✅ **必须保留**（用户清单遗漏） | 用户提到的"识别判断"（OCR 文本判断、变量比较、素材存在性）全靠它实现；loop"直到条件"的循环体出口判断也需要它。删除后流程无判断能力 |
| **navigator 导航器** | ✅ 保留 | 返回主界面（back×3）、关闭弹窗（back×1）、重启游戏（包名）都是按键动作，拖拽器无法替代。任务开头/结尾清理弹窗、失败回主界面的实用节点 |
| **set_var 变量设置** | ✅ 保留（外部参数方案的执行者） | 写死值虽鸡肋，但外部参数注入后它就是"用参数初始化变量"的入口；也是 branch/loop 判断的数据来源 |
| **counter 计数器** | ✅ 保留 | 变量 += delta，循环体内计数（循环次数、失败次数）的核心；set_var 无法自增 |
| **wait 等待** | ✅ 保留（基础节点） | 循环间隔、动画等待 |
| **loop 循环** | ✅ 保留（基础节点） | 已有"固定次数 / 直到条件"两模式 |
| **compound 复合节点** | ✅ 保留 | 框选封装子图 / 通用节点库复用，是"脚本片段复用"的载体，用户没有提及但价值独立 |
| **scroll_capture 滚动捕获** | ✅ 保留 | 用户上一轮刚确认：滚动多屏拼接全景，供标注/识别 |
| **checker 检查器** | ⚠️ **建议删除** | 旁路拦截 = branch（true/false）+ navigator/wait 的组合快捷方式，无独立能力；条件成立走 triggered（返回主菜单/等待/跳过）均可由 branch+navigator/wait 拼出 |
| **refresher 刷新器** | ⚠️ **建议删除** | 只是"下拉刷新"= 拖拽器（down 方向+距离）的一个特例，拖拽器完全覆盖 |

结论：可删除的是 **checker、refresher**（能力均被基础节点组合覆盖）；
**branch 必须补进基础清单**；navigator / set_var / counter / compound / scroll_capture 有独立使用场景，保留。

删除方式（未来执行时）：node_defs 移除条目 + nodes.py 移除执行器与注册表条目 + pan_viewer 右键示教菜单若有引用则清理；旧任务加载遇未知类型 dispatch 已返回 error 不崩（但建议删除前先检查存量任务是否使用）。

## 二、外部参数方案设计（任务参数表）

问题：脚本成型后，循环几次、识别判断（关键词/条件值）、等待秒数等只能写死在节点里，无法从外部设置。

### 方案总览

任务 JSON 增加 `params` 参数定义表；运行时外部注入实际值；
节点参数支持 `${参数名}` 引用；参数同时注入为任务变量（供 branch/loop 条件引用变量名）。

```
┌─────────────┐   ▶ 运行（弹窗填值/默认值）   ┌──────────────┐
│ task.params │ ────────────────────────────▶ │ ctx.params   │
│ 参数定义表   │                               │ ctx.vars 初始化│
└─────────────┘                               └──────┬───────┘
                                                     │ run_graph 入口
                           ┌─────────────────────────▼──────────────┐
                           │ resolve_param_refs(graph, ctx.params)：│
                           │ 所有节点参数值里的 ${name} → 实际值      │
                           └─────────────────────────────────────────┘
```

### 1. 参数定义（task JSON）

```json
"params": [
  {"name": "loop_count", "label": "挑战次数", "type": "int",
   "default": 3, "desc": "御魂/探索挑战次数"},
  {"name": "keyword", "label": "识别关键词", "type": "text",
   "default": "体力", "desc": "OCR 命中关键词"}
]
```

- `visual_schema.default_task`/`normalize_task` 补 `"params": []`（旧任务兼容，缺失补空表）

### 2. 节点参数引用语法 `${name}`

任意**文本类**节点参数值内嵌 `${参数名}`，run_graph 入口统一替换（深拷贝 graph 后替换，
不污染已保存任务文件）：

- `loop.count`：新增文本参数 `count_var`（次数引用，可选）——填参数/变量名，
  运行时取 `ctx.get_var(该名)` 作为次数（"循环几次"外部可设置）
- `ocr_reader.keyword`：`"${keyword}"` → 实际关键词
- `branch.condition/value/data_source`：直接写变量名（参数已注入为变量）或 `${name}`
- `set_var.var_value`：`"${target}"` → 初始化变量
- `wait.seconds`：加可选文本 `seconds_var` 引用（可选扩展，默认不动）
- 未匹配到参数的 `${xxx}`：原样保留，不报错

### 3. 注入链路

- `GraphContext` 增 `params: dict` 字段
- `run_graph` 入口：`ctx.vars.update(ctx.params)`（参数即变量）+ 参数引用替换
- 入口来源：
  - **手动测试运行**：面板「▶ 运行」检测到 task.params 非空 → 弹参数填写表单
    （label 提示、default 预填）→ `teach_engine.teach_run(name, params=values)`
  - **正式调度运行**：任务配置/调度器提供 `params` 值 → `VisualTask.execute(context)`
    注入；后续可与 config/task yaml 打通
  - 无外部值时全部用 default

### 4. UI 配套（最小可用）

- 画布工具栏新增「⚙ 任务参数」按钮 → 表格编辑弹窗：
  增/删/改参数（名、显示名、类型 int/text、默认值、说明），写入 task["params"]
- 「▶ 运行」时按参数定义弹表单（每参数一行，默认值预填），确定后带值运行

### 5. 兼容性

- 旧任务无 `params` → 行为完全不变
- `${name}` 只在运行时替换，保存的任务文件仍是模板（便于反复运行）
- 参数也进 `ctx.vars`，与现有 branch/loop 条件引用变量的机制天然打通
  （loop"直到条件"的 `condition` 已支持右值变量引用）

## 三、实现清单（待确认后执行）

1. `visual_schema.py`：default_task/normalize_task 增 params
2. `visual/nodes.py`：GraphContext.params + run_graph 入口解析 `${}`；
   loop 增 count_var 参数
3. `visual/node_defs.py`：loop 增 count_var；（可选）wait 增 seconds_var
4. `visual/teach_engine.py`：teach_run(name, params=None) 透传
5. `ui/visual_builder/`：参数编辑弹窗 + 运行填参表单
6. 测试：tools/verify_visual_params.py（引用替换/默认值/注入/loop 外部次数）
