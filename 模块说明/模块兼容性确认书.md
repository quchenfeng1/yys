# 模块兼容性确认书（AI 查阅用）

> **用途**：本文件专为 AI 阅读设计，用于在后续会话中快速确认各模块间的接口兼容性。
> **规则**：所有模块引用使用 `NN-模块名` 格式（如 `01-设备连接模块`）。
> **更新**：修改任一模块后，需同步更新本文件及对应模块说明书。

---

## 一、模块间方法调用矩阵

格式：`调用方 → 被调用方.方法名(参数) → 返回值`

### 1.1 执行链路（14-执行器 → 下层模块）

| 调用方 | 被调用方 | 方法 | 参数 | 返回值 | 定义在目标模块 5.2 表 |
|-------|---------|------|------|--------|:-------------------:|
| **14-执行器模块** | **01-设备连接模块** | `screenshot()` | — | `np.ndarray` | ✅ |
| **14-执行器模块** | **01-设备连接模块** | `click(x, y)` | `int, int` | `None` | ✅ |
| **14-执行器模块** | **01-设备连接模块** | `swipe(x1,y1,x2,y2,duration)` | `int*4, int` | `None` | ✅ |
| **14-执行器模块** | **01-设备连接模块** | `input_key(key)` | `str` | `None` | ✅ |
| **14-执行器模块** | **02-图像识别模块** | `find(template_name)` | `str` | `tuple` | ✅ |
| **14-执行器模块** | **02-图像识别模块** | `exists(template_name)` | `str` | `bool` | ✅ |
| **14-执行器模块** | **02-图像识别模块** | `wait(template_name)` | `str` | `tuple` | ✅ |
| **14-执行器模块** | **03-防封策略模块** | `random_offset(cx, cy, radius)` | `int,int,int` | `tuple` | ✅ |
| **14-执行器模块** | **03-防封策略模块** | `human_move_path(x1,y1,x2,y2)` | `int*4` | `list[tuple]` | ✅ |
| **14-执行器模块** | **03-防封策略模块** | `maybe_long_pause()` | — | `bool` | ✅ |
| **14-执行器模块** | **03-防封策略模块** | `get_pause_duration()` | — | `float` | ✅ |
| **14-执行器模块** | **12-日志监控中心** | `info/warning/error/debug(msg, ...)` | `str` | `None` | ✅ |

### 1.2 运行控制 → 下层模块

| 调用方 | 被调用方 | 方法 | 参数 | 返回值 | 定义在目标模块 5.2 表 |
|-------|---------|------|------|--------|:-------------------:|
| **09-运行控制中心** | **05-时间调度模块** | `get_due_tasks()` | — | `list[TaskInfo]` | ✅ |
| **09-运行控制中心** | **05-时间调度模块** | `mark_done(name, success)` | `str, bool` | `None` | ✅ |
| **09-运行控制中心** | **05-时间调度模块** | `load_state()` | — | `None` | ✅ |
| **09-运行控制中心** | **05-时间调度模块** | `save_state()` | — | `None` | ✅ |
| **09-运行控制中心** | **04-任务执行引擎** | `task.execute(context)` | `TaskContext` | `TaskResult` | ✅ |
| **09-运行控制中心** | **01-设备连接模块** | `connect(device_id)` | `str` | `bool` | ✅ |
| **09-运行控制中心** | **01-设备连接模块** | `disconnect(device_id)` | `str` | `None` | ✅ |
| **09-运行控制中心** | **15-账号管理模块** | `switch_to(account_id)` | `str` | `bool` | ✅ |
| **09-运行控制中心** | **15-账号管理模块** | `get_task_scope()` | — | `list[str]` | ✅ |
| **09-运行控制中心** | **15-账号管理模块** | `switch_next()` | — | `bool` | ✅ |
| **09-运行控制中心** | **12-日志监控中心** | `record_task_start(name)` | `str` | `None` | ✅ |
| **09-运行控制中心** | **12-日志监控中心** | `record_task_done(name, success, duration)` | `str,bool,float` | `None` | ✅ |
| **09-运行控制中心** | **12-日志监控中心** | `capture_on_error(task, step, error)` | `str,str,Exception` | `None` | ✅ |

### 1.3 任务执行引擎 → 下层模块

| 调用方 | 被调用方 | 方法 | 参数 | 返回值 |
|-------|---------|------|------|--------|
| **04-任务执行引擎** | **14-执行器模块** | `context.executor.click_image(name)` | `str` | `bool` |
| **04-任务执行引擎** | **14-执行器模块** | `context.executor.wait_any(names)` | `list[str]` | `tuple` |
| **04-任务执行引擎** | **02-图像识别模块** | `context.recognizer.find(name)` | `str` | `tuple` |
| **04-任务执行引擎** | **06-配置管理中心** | `get_task_config(name)` | `str` | `dict` |

### 1.4 传参模块 → 下层模块

| 调用方 | 被调用方 | 方法 | 参数 | 返回值 |
|-------|---------|------|------|--------|
| **10-参数桥接模块** | **06-配置管理中心** | `get(key_path, default)` | `str, Any` | `Any` |
| **10-参数桥接模块** | **06-配置管理中心** | `set(key_path, value)` | `str, Any` | `None` |
| **10-参数桥接模块** | **05-时间调度模块** | `update_next_run(name, time)` | `str, datetime` | `None` |

### 1.5 账号管理 → 下层模块

| 调用方 | 被调用方 | 方法 | 参数 | 返回值 |
|-------|---------|------|------|--------|
| **15-账号管理模块** | **01-设备连接模块** | `switch_device(id)` | `str` | `bool` |
| **15-账号管理模块** | **06-配置管理中心** | `get_account_config(id)` | `str` | `dict` |

### 1.6 UI/传参 → 下层模块

| 调用方 | 被调用方 | 方法 | 参数 | 返回值 |
|-------|---------|------|------|--------|
| **11-用户界面模块** | **10-参数桥接模块** | `bind(widget, config_path)` | `QWidget, str` | `None` |
| **11-用户界面模块** | **10-参数桥接模块** | `bind_state(widget, state_key)` | `QWidget, str` | `None` |
| **11-用户界面模块** | **10-参数桥接模块** | `request_start/stop/pause/resume()` | — | `None` |
| **11-用户界面模块** | **13-任务文件管理** | `scan_all()` | — | `list[TaskMeta]` |
| **11-用户界面模块** | **13-任务文件管理** | `get_all_tasks()` | — | `list[TaskMeta]` |
| **11-用户界面模块** | **13-任务文件管理** | `get_tasks_by_category(cat)` | `str` | `list[TaskMeta]` |
| **11-用户界面模块** | **13-任务文件管理** | `new_task(category, name, display)` | `str,str,str` | `str` |
| **11-用户界面模块** | **13-任务文件管理** | `delete_task(module)` | `str` | `None` |
| **11-用户界面模块** | **13-任务文件管理** | `open_file(module)` | `str` | `None` |

---

## 二、事件通信矩阵

### 2.1 事件发布者 → 事件名 → 订阅者

| 事件名 | 发布者 | 订阅者 | 触发时机 |
|--------|-------|--------|---------|
| `start_requested` | **10-参数桥接模块** | **09-运行控制中心** | 用户点击"启动" |
| `stop_requested` | **10-参数桥接模块** | **09-运行控制中心** | 用户点击"停止" |
| `pause_requested` | **10-参数桥接模块** | **09-运行控制中心** | 用户点击"暂停" |
| `resume_requested` | **10-参数桥接模块** | **09-运行控制中心** | 用户点击"恢复" |
| `run_started` | **09-运行控制中心** | **11-用户界面模块** + 其他 | 运行已开始 |
| `run_paused` | **09-运行控制中心** | **11-用户界面模块** + 其他 | 运行已暂停 |
| `run_stopped` | **09-运行控制中心** | **11-用户界面模块** + 其他 | 运行已停止 |
| `run_limit_reached` | **09-运行控制中心** | **09-运行控制中心**（自处理） | 每日时长/操作次数达上限 |
| `connection_lost` | **01-设备连接模块** | **11-用户界面模块** | 连接断开 |
| `connection_restored` | **01-设备连接模块** | **11-用户界面模块** | 连接恢复 |
| `connection_error` | **01-设备连接模块** | **11-用户界面模块** | 连接错误 |
| `task_started` | **04-任务执行引擎** | **11-用户界面模块** | 任务开始执行 |
| `task_done` | **04-任务执行引擎** | **11-用户界面模块** | 任务执行完成 |
| `step_done` | **04-任务执行引擎** | **11-用户界面模块** | 步骤执行完成 |
| `task_skipped` | **05-时间调度模块** | **11-用户界面模块** | 任务被跳过 |
| `schedule_updated` | **05-时间调度模块** / **09-运行控制中心** | **11-用户界面模块** + **09-运行控制中心** | 日程表更新 / 队列刷新后通知 UI 更新展示 |
| `task_due` | **05-时间调度模块** | — | 任务到期（预留） |
| `daily_reset` | **05-时间调度模块** | 模块内部 | 凌晨 00:00 重置 |
| `log_record` | **12-日志监控中心** | **11-用户界面模块** | 新日志产生 |
| `state_changed` | **07-运行时状态管理** | **11-用户界面模块** + **10-参数桥接模块** | 任意状态变更 |
| `config_changed` | **06-配置管理中心** | 各模块（按需过滤） | 配置被修改 |
| `account_switched` | **15-账号管理模块** | **01-设备连接模块** + **05-时间调度模块** + **07-运行时状态管理** | 账号切换 |

### 2.2 发布者缺失校验

| 事件在 08 目录 | 发布者在自身文档中声明了 `_event_bus` | 状态 |
|---------------|--------------------------------------|:----:|
| 全部 22 个事件 | 各发布者均已声明 | ✅ |

### 2.3 订阅者声明校验

| 订阅者 | 订阅的事件 | 在自身文档中声明 | 状态 |
|-------|-----------|:--------------:|:----:|
| **09-运行控制中心** | start_requested, stop_requested, pause_requested, resume_requested, run_limit_reached | ✅ | ✅ |
| **11-用户界面模块** | task_started, task_done, step_done, state_changed, log_record, schedule_updated, connection_lost, connection_restored | ✅ | ✅ |
| **10-参数桥接模块** | state_changed | ✅ | ✅ |

---

## 三、状态键读写矩阵

### 3.1 完整状态键表

| 类别 | 状态键 | 类型 | 写入方 | 读取方 |
|------|--------|------|--------|--------|
| **运行状态** | `run_status` | `RunState` | **09-运行控制中心** | **11-用户界面模块** |
| | `run_start_time` | `datetime\|None` | **09-运行控制中心** | **12-日志监控中心** |
| | `today_run_duration` | `float` | **09-运行控制中心** | **11-用户界面模块** |
| | `run_summary` | `dict\|None` | **09-运行控制中心** | **11-用户界面模块** |
| **队列状态** | `schedule_queue` | `list[TaskInfo]` | **05-时间调度模块** | **11-用户界面模块** |
| | `task_status` | `dict[str, ScheduleStatus]` | **05-时间调度模块** | **11-用户界面模块** |
| | `queue_length` | `int` | **09-运行控制中心** | **11-用户界面模块** |
| | `queue_contents` | `list[str]` | **09-运行控制中心** | **11-用户界面模块** |
| **任务执行** | `current_task` | `str\|None` | **09-运行控制中心** / **04-任务执行引擎** | **11-用户界面模块** |
| | `current_step` | `str\|None` | **04-任务执行引擎** | **11-用户界面模块** |
| | `task_progress` | `str\|None` | **04-任务执行引擎** | **11-用户界面模块** |
| **账号状态** | `current_account` | `str` | **15-账号管理模块** | **11-用户界面模块** |
| | `account_role` | `str` | **15-账号管理模块** | **09-运行控制中心**（填充线程据此过滤任务范围） |
| **连接状态** | `connection_status` | `ConnectionState` | **01-设备连接模块** | **11-用户界面模块** |
| | `active_device_id` | `str\|None` | **01-设备连接模块** | **15-账号管理模块** |
| **场景状态** | `current_scene` | `str\|None` | **04-任务执行引擎** | **11-用户界面模块** |
| **熔断状态** | `circuit_breaker_tripped` | `bool` | **09-运行控制中心** | — |
| **安全状态** | `today_operation_count` | `int` | **09-运行控制中心** | — |
| | `run_limit_reached` | `bool` | **09-运行控制中心** | — |

---

## 四、模块依赖注入图（构造函数依赖）

格式：`模块名 → 依赖的模块（通过构造函数注入）`

| 模块 | 依赖注入的模块 |
|------|---------------|
| **01-设备连接模块** | `06-配置管理中心`, `08-事件通信总线`, `07-运行时状态管理` |
| **02-图像识别模块** | `01-设备连接模块`, `06-配置管理中心`, `12-日志监控中心` |
| **03-防封策略模块** | `06-配置管理中心`, `12-日志监控中心` |
| **04-任务执行引擎** | `06-配置管理中心`, `08-事件通信总线`, `07-运行时状态管理` |
| **05-时间调度模块** | `06-配置管理中心`, `07-运行时状态管理`, `08-事件通信总线` |
| **06-配置管理中心** | `08-事件通信总线`, `12-日志监控中心` |
| **07-运行时状态管理** | `08-事件通信总线` |
| **08-事件通信总线** | `12-日志监控中心`（可选） |
| **09-运行控制中心** | `05-时间调度模块`, `04-任务执行引擎`, `01-设备连接模块`, `06-配置管理中心`, `07-运行时状态管理`, `08-事件通信总线`, `12-日志监控中心` |
| **10-参数桥接模块** | `06-配置管理中心`, `08-事件通信总线`, `07-运行时状态管理`, `05-时间调度模块` |
| **11-用户界面模块** | `10-参数桥接模块`, `08-事件通信总线`, `04-任务执行引擎`（间接） |
| **12-日志监控中心** | `08-事件通信总线`, `06-配置管理中心`, `01-设备连接模块` |
| **13-任务文件管理** | 无（纯数据层） |
| **14-执行器模块** | `02-图像识别模块`, `03-防封策略模块`, `01-设备连接模块`, `12-日志监控中心`, `06-配置管理中心` |
| **15-账号管理模块** | `06-配置管理中心`, `01-设备连接模块`, `07-运行时状态管理`, `08-事件通信总线`, `05-时间调度模块` |
| **16-应用启动引导** | 无（创建者，非使用者） |

---

## 五、模块分类与影响域

### 5.1 三层架构

| 层级 | 包含模块 | 特征 |
|------|---------|------|
| 🏗️ **桥梁表现层** | `10-参数桥接模块`, `11-用户界面模块` | UI 交互，通过 10 桥接核心 |
| ⚙️ **支撑服务层** | `06-配置管理中心`, `07-运行时状态管理`, `08-事件通信总线`, `12-日志监控中心`, `13-任务文件管理`, `15-账号管理模块`, `16-应用启动引导` | 基础设施，被核心层依赖 |
| 🎯 **核心运行层** | `01-设备连接模块`, `02-图像识别模块`, `03-防封策略模块`, `04-任务执行引擎`, `05-时间调度模块`, `09-运行控制中心`, `14-执行器模块` | 业务逻辑，依赖支撑层 |

### 5.2 修改影响域规则

当修改模块 X 时，以下模块需同步检查：

| 若修改 | 需同步检查 | 原因 |
|-------|-----------|------|
| **01-设备连接模块** | `14-执行器模块`, `09-运行控制中心`, `15-账号管理模块`, `02-图像识别模块`, `12-日志监控中心` | 这些模块调用 01 的方法或注入 01 的依赖 |
| **02-图像识别模块** | `14-执行器模块`, `04-任务执行引擎` | 调用 02 的方法 |
| **03-防封策略模块** | `14-执行器模块` | 调用 03 的方法 |
| **04-任务执行引擎** | `09-运行控制中心` | 调用 04 的方法；04 发布事件 UI 订阅 |
| **05-时间调度模块** | `09-运行控制中心`, `10-参数桥接模块`, `15-账号管理模块` | 调用 05 的方法 |
| **06-配置管理中心** | **所有模块** | 全部 16 个模块均依赖 06 读配置 |
| **07-运行时状态管理** | **所有模块** | 全部模块均通过 07 读写状态 |
| **08-事件通信总线** | **所有模块** | 全部模块均通过 08 发布/订阅事件 |
| **09-运行控制中心** | `05-时间调度模块`, `04-任务执行引擎`, `01-设备连接模块`, `15-账号管理模块`, `08-事件通信总线`, `12-日志监控中心` | 09 调用这些模块并订阅 08 事件；09 发布事件 UI 订阅 |
| **10-参数桥接模块** | `11-用户界面模块`, `06-配置管理中心`, `05-时间调度模块` | UI 通过 10 桥接；10 调用 06/05 |
| **11-用户界面模块** | `10-参数桥接模块`, `08-事件通信总线`, `13-任务文件管理` | UI 通过 10 交互；订阅 08 的事件；通过 13 管理任务文件 |
| **12-日志监控中心** | `02-图像识别模块`, `03-防封策略模块`, `09-运行控制中心`, `14-执行器模块` | 这些模块调用 12 的方法 |
| **13-任务文件管理** | `11-用户界面模块`, `16-应用启动引导` | UI 调用 13 的方法 |
| **14-执行器模块** | `04-任务执行引擎`, `02-图像识别模块`, `03-防封策略模块`, `01-设备连接模块` | 04 通过 14 执行操作；14 调用 02/03/01 |
| **15-账号管理模块** | `09-运行控制中心`, `01-设备连接模块`, `05-时间调度模块`, `07-运行时状态管理` | 09 调用 15；15 调用 01/05 |
| **16-应用启动引导** | **全部模块** | 16 负责创建所有模块实例，修改依赖注入顺序影响全局 |

---

## 六、初始化顺序

按依赖关系排序（被依赖方先初始化）：

```
第1层（基础设施，无依赖）：
  ① 06-配置管理中心  → 加载全部配置
  ② 08-事件通信总线  → 创建全局单例

第2层（基础服务）：
  ③ 12-日志监控中心  → 注入 06, 08
  ④ 07-运行时状态管理 → 注入 08

第3层（核心功能）：
  ⑤ 01-设备连接模块  → 注入 06, 08, 07
  ⑥ 02-图像识别模块  → 注入 01, 06, 12
  ⑦ 03-防封策略模块  → 注入 06, 12

第4层（业务编排）：
  ⑧ 05-时间调度模块  → 注入 06, 07, 08
  ⑨ 14-执行器模块    → 注入 02, 03, 01, 12, 06
  ⑩ 04-任务执行引擎  → 注入 06, 08, 07
  ⑪ 13-任务文件管理  → 无依赖

第5层（参数桥接）：
  ⑫ 10-参数桥接模块  → 注入 06, 08, 07, 05（05 已就绪）

第6层（运行控制）：
  ⑬ 09-运行控制中心  → 注入 05, 04, 01, 06, 07, 08, 12

第7层（账号与界面）：
  ⑭ 15-账号管理模块  → 注入 06, 01, 07, 08, 05
  ⑮ 11-用户界面模块  → 注入 10, 08, 04（间接）

关闭顺序：⑮ → ⑭ → ⑬ → ⑫ → ⑪ → ⑩ → ⑨ → ⑧ → ⑦ → ⑥ → ⑤ → ④ → ③ → ② → ①
```

---

## 七、兼容性断言检查清单

以下断言在 AI 修改模块后必须逐条验证：

### 7.1 方法签名断言

```
□ 14-执行器 调用的 01.click(x,y) 签名未变
□ 14-执行器 调用的 02.find(name) 签名未变
□ 14-执行器 调用的 03.random_offset(cx,cy,radius) 签名未变
□ 09-运行控制 调用的 05.get_due_tasks() 返回值仍是 list[TaskInfo]
□ 09-运行控制 调用的 05.mark_done(name,success) 签名未变
□ 09-运行控制 调用的 04.task.execute(context) 返回值仍是 TaskResult
□ 09-运行控制 调用的 15.switch_to(account_id) 返回 bool
□ 09-运行控制 调用的 15.get_task_scope() 返回 list[str]
□ 09-运行控制 调用的 15.switch_next() 返回 bool
□ 14-执行器 调用的 12.log/info/warning/error/debug 签名未变
□ 10-参数桥接 调用的 06.get/set 签名未变
□ 15-账号管理 调用的 01.switch_device(id) 签名未变
```

### 7.2 事件断言

```
□ 06 修改后仍能发布 config_changed 事件
□ 07 修改后仍能发布 state_changed 事件
□ 08 修改后仍能正常分发所有事件
□ 10 修改后仍能发布 start/stop/pause/resume_requested 事件
□ 09 修改后仍能发布 run_started/paused/stopped 事件
□ 01 修改后仍能发布 connection_lost/restored/error 事件
□ 04 修改后仍能发布 task_started/done/step_done 事件
□ 05 修改后仍能发布 schedule_updated/task_due/daily_reset/task_skipped 事件
□ 15 修改后仍能发布 account_switched 事件
□ 12 修改后仍能发布 log_record 事件
```

### 7.3 状态键断言

```
□ 01 修改后仍能写 connection_status + active_device_id
□ 04 修改后仍能写 current_task + current_step + task_progress + current_scene
□ 05 修改后仍能写 schedule_queue + task_status
□ 09 修改后仍能写 run_status + run_start_time + today_run_duration + run_summary + queue_length + queue_contents + circuit_breaker_tripped + today_operation_count + run_limit_reached
□ 15 修改后仍能写 current_account + account_role
```

### 7.4 配置路径断言

```
□ 06.get("global.adb.*") 路径不变（01 读取）
□ 06.get("tasks.*.*") 路径不变（04/05 读取）
□ 06.get("accounts.*") 路径不变（15 读取）
□ 06.get("global.anti_detect.*") 路径不变（03 读取）
```

---

## 八、附录：模块文件清单

| 编号 | 文件名 | 模块说明书路径 |
|-----|--------|---------------|
| 01 | 设备连接模块 | `模块说明/01-设备连接模块.md` |
| 02 | 图像识别模块 | `模块说明/02-图像识别模块.md` |
| 03 | 防封策略模块 | `模块说明/03-防封策略模块.md` |
| 04 | 任务执行引擎 | `模块说明/04-任务执行引擎.md` |
| 05 | 时间调度模块 | `模块说明/05-时间调度模块.md` |
| 06 | 配置管理中心 | `模块说明/06-配置管理中心.md` |
| 07 | 运行时状态管理 | `模块说明/07-运行时状态管理.md` |
| 08 | 事件通信总线 | `模块说明/08-事件通信总线.md` |
| 09 | 运行控制中心 | `模块说明/09-运行控制中心.md` |
| 10 | 参数桥接模块 | `模块说明/10-参数桥接模块.md` |
| 11 | 用户界面模块 | `模块说明/11-用户界面模块.md` |
| 12 | 日志监控中心 | `模块说明/12-日志监控中心.md` |
| 13 | 任务文件管理 | `模块说明/13-任务文件管理.md` |
| 14 | 执行器模块 | `模块说明/14-执行器模块.md` |
| 15 | 账号管理模块 | `模块说明/15-账号管理模块.md` |
| 16 | 应用启动引导 | `模块说明/16-应用启动引导.md` |
| — | 模块设计书规范 | `模块说明/模块设计书规范.md` |
| — | 模块拼接书 | `模块说明/模块拼接书.md` |
| — | 模块兼容性确认书 | `模块说明/模块兼容性确认书.md`（本文件） |
