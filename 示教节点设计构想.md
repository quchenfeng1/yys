# 示教节点设计构想（记录 + 可行性分析）

> 记录时间：2026-08-14
> 来源：用户口述，由 AI 整理。本文件为设计讨论底稿，非最终方案。

## 一、用户原始设想（口述要点）

### 1. 示教节点（核心新概念）
- 新增一个「示教节点」，进入示教界面设置好后保存。
- 该节点**向外输出截图的各种处理结果**。
- 当前输出仅两类：**红框**、**蓝框+遮罩**。
- 一个示教节点可能有**多个选框**（多个红框 / 多个蓝框）。

### 2. 基础识图节点的输入
- 所有需要识图的模块，新增两个输入：**特征**、**区域**。
- **特征** = 蓝框内所有遮罩涂出的图标（要识别的东西）。
- **区域** = 红框的范围（在模拟器屏幕的哪个范围去找特征）。

### 3. 五种解耦后的基础节点

| 节点 | 功能 | 输出 |
|------|------|------|
| **识别器** | 判断区域内是否有特征 | true / false |
| **点击器** | 点击区域内的特征图标（可放识别器后，识别到再点） | 成功 true / 没找到 false |
| **OCR 识别器** | 识别目标区域中特征关联文字（蓝框内加**黄框**，识别到特征后去对应黄框位置提取文字） | 提取的文字 |
| **判断器** | 对文字用**正则等高级判断**（不只等于） | 成功 true / 失败 false；可用于循环（后续节点 out 接回前面节点） |
| **特征数量识别器** | 识别目标区域内的特征数量 | 数量 |

### 4. 用户自述
> "我的思路有些乱了，先帮我记录。"

## 二、结构化理解

用户的本质意图：

```
现状：每个识图节点各自配置【模板 + 区域】（参数化，节点间不共享）
目标：示教节点产出【特征 + 区域】数据，识图节点接收（数据流，一处示教多处复用）
```

数据流向（设想）：

```
[示教节点] --特征(蓝框遮罩图标)--> [识别器/点击器/OCR/计数]
           --区域(红框范围)------> [识别器/点击器/OCR/计数]

[识别器] --true/false--> [判断器/循环回跳]
[OCR识别器] --文字--> [判断器(正则)]
```

## 三、与现有体系的映射（可行性判断）

| 用户设想节点 | 对应现有节点 | 差距 |
|-------------|-------------|------|
| 识别器 | `scene_probe` / `matcher`（判断有无） | 语义已具备，差"接收外部特征+区域" |
| 点击器 | `matcher` + `clicker(识别坐标)` | 已具备"识别到再点击中心" |
| OCR 识别器 | `ocr_reader` | 已具备；差"黄框关联区域" |
| 判断器 | `branch` | 差"正则/高级比较"（现有仅 ==/!=/>=/<=/存在） |
| 特征数量识别器 | `matcher`（index 多实例） | 差"返回数量"而非"取第 N 个" |

结论：**不是全新造一套，而是把现有节点拆细 + 用「特征/区域」数据流串起来**。

## 四、待澄清的关键问题（实现前必须拍板）

1. **示教节点形态**：
   - A) 图里的一个节点，输出特征/区域数据（数据流连线）
   - B) 素材库（现有 SceneStore），识图节点下拉引用素材 ID（现状）
   - C) 变量引用（示教输出到变量，识图节点引用变量名）

2. **特征/区域如何传递**：数据端口连线？变量名？素材 ID？

3. **一个示教节点多组特征/区域时**，识别器是"处理全部"还是"指定其中一组"？输出什么？

4. **点击器与识别器的依赖**：控制流顺序（识别器 out → 点击器 in）还是数据流（识别器结果 → 点击器）？

5. **黄框**：蓝框内加黄框 = 特征图标关联的文字区域，需要扩展示教数据结构
   `蓝框 = { 遮罩图标, 黄框(文字区域) }`。

6. **判断器循环**：复用现有 `loop` + `loop_back` 机制，还是判断器自带循环？

## 五、初步可行性结论

**总体可行，方向正确**（从"参数化配置"演进为"数据流传递"，解耦更彻底、复用更强）。

关键设计点：
- 优先复用现有 SceneStore / matcher / ocr_reader / branch，避免推倒重来。
- 「特征 + 区域」建议做成**数据端口连线**（NodeGraphQt 原生支持数据端口）。
- 「判断器」在现有 branch 上增加正则等高级比较即可。
- 「特征数量识别器」= matcher 多实例匹配的计数版。
- 「黄框」是示教数据结构的一次扩展（蓝框内嵌套黄框）。

## 六、截图控制模式（2026-08-14 追加）

用户新增构想：**只有场景判定（识图）节点能控制截图**，防止模拟器无目的截图浪费性能。

搭图约定（状态机模式）：

```
识图1（前置判断：在目标界面）→ 操作（点击/滑动）→ 识图2（结果判断：到达目标界面）
```

- 每个操作节点前后各放一个识图节点。
- 识图2 同时作为「本次操作的结果校验」和「下一个操作的前置判断」。
- 点击未生效 → 识图2 判定失败走 false → 连线回跳识图1 重新判定，形成重试循环。

分析结论：
- ✅ 逻辑清晰、截图次数可控，是好的搭图规范（显式状态机思想）。
- ⚠️ 回跳需注意死循环：现有执行器对普通节点回跳无循环计数（仅 max_steps 兜底），
  建议用 `loop` 节点包裹「识图1→操作→识图2」，或用「计数器 + 判断器」限制重试次数。
- ℹ️ 现状：操作节点（clicker/dragger 等）本就不截图，仅识图类节点（matcher/scene_probe/ocr）截图，
  该约定在代码层面已基本成立，主要作为搭图规范固化。
- 💡 识图1 与识图2 可复用同一特征（特征=目标按钮/页面变化），也可用不同特征。

## 七、失效判断端口设想（2026-08-14 追加）

用户新增构想：给识图节点加「失效判断输入/输出」端口。

```
识图1.失效判断输出 ──> 识图2.失效判断输入
识图2 自身特征失败时 → 转而用识图1 的特征再判断一次：
  - 识图1 特征命中（画面没变，点击未生效）→ 走回点击功能（重试）
  - 识图1 特征也失败（画面既不是1也不是2）→ 异常
```

分析结论：
- ✅ 核心价值：能区分「点击未生效（画面没变）」vs「画面已变但特征没匹配」，对重试判断有用。
- ⚠️ 实现更简单的等价方案：**复用同一特征 + 普通识图节点**，不必新增专用端口。
  `识图2.miss → 识图(按钮还在?) → true 回跳 loop_back / false 走异常`
  其中「按钮还在」与识图1 用同一个特征素材即可。
- 💡 若坚持数据流：本质是「特征从上游传下游」，应做成通用的**特征数据端口**（见第五节），
  而非专用「失效判断」端口——专用端口会让连线在多层借用时越来越乱。

## 八、截图/识图分离 + 信号端口统一（2026-08-14 追加）

用户构想：
1. **截图与识图分离**：同一页面不同状态只需多次判断，不必多次截图。
2. **信号输入/输出**：每个基础节点加「信号输入 + 信号输出」；开始只有信号输出、
   结束只有信号输入；判断节点成功→true 输出信号、失败→false 输出信号。
3. **OCR 节点双输出**：一个信号 + 一个识别值（识别值传入判断节点）。
4. **去掉循环节点**：靠信号回跳成环实现循环（保留「环状节点做循环」思路）。

分析结论：
- ✅ 截图分离是真实优化：建议「截图节点刷新缓存帧 + 识图节点读缓存帧」，
  一次截图多次判断。
- ✅ 信号输入/输出统一，模型更规整（等价于把 out/not_found/miss 统一为 true/false）。
- ⚠️ 矛盾点：「保留环状节点」与「不需要循环节点」二选一。去掉 loop 后，回跳循环
  失去计数/条件保护，死循环只能靠 max_steps 兜底；建议 loop 保留作「有边界循环」，
  信号回跳作「简单重试」快捷方式，二者共存。
- ⚠️ OCR 双输出 = 信号流 + 数据流混合，需要明确「数据端口」机制（识别值如何连线传入
  判断节点），与第五节「特征数据端口」合并设计。

## 九、状态转移验证节点（2026-08-14 追加）

用户洞察：识图本质是「两张图的对比」= 两个特征的状态转移：

```
特征1 识别 → 操作 → 识别特征2
  ├ 特征2 识别到 → 成功(out)
  └ 特征2 未识别 → 回判特征1
        ├ 特征1 还在 → 再操作（retry，重试）
        └ 两者都没识别 → 异常(unknown，走示教/报错)
```

封装为单个基础节点（建议名：状态转移 / 操作验证）。

参数：特征1、特征2、操作（点击点/滑动）、重试次数。
端口：in / out(到达特征2) / retry(还在特征1) / unknown(两者皆无，接示教)。

结论：✅ 正确的抽象，覆盖游戏自动化最高频的「点击 → 验证跳转」模式；
封装后一个节点替代「识图1+操作+识图2+回判识图1+重试」整条链。

## 十、场景状态机架构（2026-08-14 追加，重大思路转变）

用户重新梳理：**脚本不是一条线，而是「场景状态机」**。

- 每个任务有若干固定画面（场景）；只要还在这几个场景中 = 未脱离任务。
- 从每个场景出发操作，操作后识图，识别出哪个场景就跳转到该场景继续下一步。
- 每个场景的操作/判断逻辑完全分开。
- 操作后还在原场景 → 重试；连续识别出 N 次同一场景 → 报错。
- 识图结果不在任何已知场景 → 异常（脱离任务，接示教）。

结论：✅ 这是游戏自动化最成熟的「有限状态机 + 场景识别」架构，方向正确。

落地关键（待实现）：
- 「多场景识别」：一次截图匹配所有场景，返回命中场景 id（现有 scene_probe 是单场景判定，需扩展或新增）。
- 异常 = 不在任何已知场景 → 接示教（on_unknown）。
- 重试上限 = 连续 N 次识别出同一场景 → 报错/异常处理。

## 十一、场景状态机·最终设计定稿（2026-08-14）

> 用户已把思路捋清，以下为完整架构。**换设备续接时以本节为准。**

### 核心思想
脚本不是一条线，而是「有限状态机」：每个任务有若干固定画面（场景），
只要还在这些场景中 = 未脱离任务。从每个场景出发操作，操作后识图，
识别出哪个场景就跳转到该场景继续。

### 架构分层

1. **中央识别层：场景信号表**
   - 一个「区域特征 → 场景信号」的映射表（动态列表，场景数量可变）。
   - 一次截图匹配所有特征，命中哪个特征就输出对应场景信号（推到外部）。
   - 与单场景识别（scene_probe，输出 true/false）的区别：信号表做多分类，输出场景 id。
   - 全无命中 → 异常信号（脱离任务）。

2. **场景任务层：场景起始节点 + 场景内操作**
   - 每个场景一个「场景起始节点」，声明监听自己的场景信号；信号对上则激活该场景任务。
   - 场景内操作：大部分是单点击（点完进入其他画面）；也有「点击后判断开关是否打开」、
   或 OCR 识别出体力不足走不同操作（场景内分支）。

3. **跳转优化：期望页面快速路径**
   - 跳转一般是固定的 → 操作后先查「期望页面」（单场景识别，开销小），命中直接固定跳转。
   - 只有没识别到期望页面时，才回查全局信号表：判断仍在任务内哪个场景 / 还是跳出任务异常。

### 数据流（示意）

```
[场景信号表] --场景信号--> 激活对应[场景起始节点]
[场景起始节点] --操作--> [期望页面检查(scene_probe)]
  ├ 命中期望 --> 固定跳转到下一场景起始
  └ 未命中 --> 回查[场景信号表]
       ├ 命中某场景 --> 激活该场景（异常跳转但仍在任务内）
       └ 全无命中 --> 异常(跳出任务) --> 接示教
```

### 落地映射（复用 vs 新增）

| 概念 | 落地方案 |
|------|---------|
| 场景信号表 | **新增**：多场景识别器（复用 SceneStore + _match_template） |
| 信号分发/激活 | **复用** event_bus（信号名匹配激活场景起始节点） |
| 场景起始节点 | **新增** scene_entry（声明监听场景 id） |
| 期望页面检查 | **复用** scene_probe（单场景判定） |
| 场景内判断 | **复用** ocr_reader + branch/checker |
| 单点击操作 | **复用** clicker |

### 待拍板的 3 个细节（下次会话继续）
1. 场景信号表结构：一个场景挂一个还是多个特征？（建议：可挂多个，任一命中即判定该场景）
2. 多场景同时命中优先级：（建议：信号表顺序取第一个，或取匹配分数最高）
3. 异常判定语义：未命中期望 → 回查全局 → 命中其他场景=仍在任务内跳转；全无命中=异常接示教。

### 之前讨论中已收敛的结论（备查）
- 截图与识图分离：截图节点刷新缓存帧，识图节点读缓存帧（一次截图多次判断）。
- 信号端口统一：节点 = 信号输入 + 成功/失败信号输出（等价于统一 out/not_found/miss）。
- loop 节点保留：作「有边界的循环」（战斗次数等大循环）；「点击失败小回调」用
  识图节点内置 retry 端口 + 重试次数参数，不用 loop。
- 状态转移验证节点（特征1→操作→特征2）已被「场景状态机」架构吸收，不再单独立项。

## 十二、截图器 + 节点解耦设计（2026-08-15 定稿）

> 用户新构想：截图权集中到「截图器」节点，识图/操作节点全部改为端口输入，
> 彻底解耦「截图、识别、操作」三层。本节已拍板定稿。

### 已拍板决策

1. **截图权唯一**：只有「截图器」节点可触发屏幕截图，写入帧缓存。
2. **自动清帧**：任何操作节点（点击/滑动/导航等）执行后自动清帧，
   强制下次识图前先过截图器（防止读到操作前的旧画面）。
3. **滚动捕获用循环**：`scroll_capture` 不再自行截图，改为读帧缓存 +
   拖拽 + 拼接保存；用户搭「循环 → 截图器 → 拖拽器」实现滚动。
4. **点击器融合（识别并随其操作）**：点击器直接接收「图标素材 + 区域」，
   节点内先识别后点击；纯识别用途保留 `matcher`（输出坐标，供判断/分支用）。
5. **素材节点**：基础节点中新增独立于所有节点的「素材节点」，
   素材全部储存在识别素材文件夹，有唯一标识；素材设置好后出现在各节点
   素材下拉框中（场景素材 / 图标素材）。

### 节点体系（解耦后）

| 节点 | 信号输入 | 数据输入 | 信号输出 | 数据输出 | 说明 |
|------|---------|---------|---------|---------|------|
| 截图器 screenshot | in | — | out | frame（截图） | **唯一截图权**，写帧缓存 |
| 素材节点(场景) | — | — | — | scene_id | 场景素材：识别区域+标志组（示教编辑） |
| 素材节点(图标) | — | — | — | icon_id | 图标素材：仅标识图（示教编辑） |
| 场景识别器 | in | frame + 场景素材id | out / not_found | 命中场景 | 场景=信号列表：n 标志命中 m 个(≥accuracy) |
| 标志识别器 matcher | in | frame + 图标素材id + 区域 | out / miss | 坐标(x,y,w,h) | 纯识别（不点击） |
| 点击器 | in | 图标素材id + 区域（或固定点） | out | 点击结果 | 融合模式：区域内识别图标并点击 |
| OCR识别器 | in | frame + 文字区域 | out / miss | 文字 | 区域文字提取 |
| 判断器 | in | 变量/文字 | true / false | — | 正则/比较 |
| 循环 | in / loop_back | 循环变量（可输出区域） | out / done | — | 循环变量可传入区域 |

### 区域确定优先级（点击器 / 标志识别器）

1. **外部输入**（数据端口 / 循环变量）——有输入优先使用
   （目的：不同循环逻辑每次识别区域不同，由循环节点输出传入）
2. **节点自身配置**（示教手画）——打开示教界面自动截一张图，画一个区域
3. **都没有 → 全图搜索**

### 素材体系

- 素材储存在识别素材文件夹，**唯一标识**（scene_id / icon_id）。
- 场景素材 = 识别区域（红框）+ 标志组（蓝框遮罩块，一个场景 n 个标志，
  命中 m 个 ≥ accuracy → true）。
- 图标素材 = 单标识图（无区域，区域由使用方决定）。
- 素材节点在图中放置后可打开示教编辑；设置好后各识图/点击节点的
  素材下拉框（combo_scene / combo_element）即可选用。

### 与现有体系映射

| 构想 | 现有 | 差距 |
|------|------|------|
| 截图器 | ctx.screenshot() 帧缓存（shot_ttl 0.2s） | 新增节点；帧缓存改 ctx.frame 语义 |
| 自动清帧 | 操作节点无清帧逻辑 | 操作节点执行后 ctx.clear_frame() |
| 场景识别器 | scene_probe + _judge_scene_v2 | ✅ 语义已具备；差「读帧」 |
| 点击器融合 | clicker(识别坐标) + matcher 组合 | 合并为「图标+区域」输入模式 |
| 素材节点 | SceneStore + 素材管理面板 | 图中新增素材代理节点 |
| 区域优先级 | matcher.region 参数 | 端口输入 > 参数 > 全图 |
| 滚动捕获 | scroll_capture 内部截图 | 改为读帧缓存 |

### 落地顺序（实施计划）

1. 帧缓存改造：ctx.frame（截图器写入）+ 操作节点自动清帧 + 旧节点 fallback
2. 截图器节点（screenshot）+ 帧数据端口
3. 素材节点（场景/图标，示教编辑入口）
4. 点击器「图标+区域」融合模式 + 区域三级优先级
5. 场景识别器/标志识别器/OCR 读帧改造
6. scroll_capture 改为读帧缓存（循环方案）
7. frame_diff 帧对比节点（第十三节拍板：拖到底检测）
8. 回归验证 + UI 节点库更新

### 兼容策略

- 旧任务无截图器：识图节点读帧为空时 fallback 自行截图（现行为不变）。
- 旧 scene_probe/matcher/clicker 参数结构不变，仅新增端口输入。

## 十三、列表击破任务·状态机分析（2026-08-15）

> 用户疑问：一个"目标列表"逐点击破的任务，标志分 1（可攻击）/ 2（打不过）/
> 0（已击败移出屏幕）。用户设想了一个"循环中的触发分支"节点（非 loop 节点，
> 通过不同信号决定不同输出）。本节分析如何用现有节点实现、缺什么能力。

### 场景建模（把所有情况归纳为 3 态决策）

用户的全部情况（含 2 2 1、全 2、到底剩 2 0 等）可归纳为每轮一次的三态判定：

| 屏幕状态 | 动作 | 之后 |
|---------|------|------|
| 有 1 | 点击第一个 1（攻击） | 回到识别（1→0 移出，列表顶上） |
| 无 1 但有 2 | 拖屏（寻找下方的 1） | 回到识别 |
| 无 1 无 2（全清 / 拖到底） | 结束任务 | — |

关键洞察：
- 「击杀后 0 移出屏幕」天然保证列表头部恒为第一个 1 → `matcher(icon_1, index=0)`
  （按位置排序取首个）即可锁定攻击目标，无需顺序计算。
- 状态判断只需两个**存在性**：有没有 1、有没有 2——不需要计数。

### 现有节点可行性（主循环零新节点）

```
loop（直到结束）
 ├─ out → matcher(icon_1, index=0)
 │         ├ out(有1) → clicker(识别坐标) → loop_back      ← 攻击第一个 1
 │         └ miss → matcher(icon_2, index=0)
 │                    ├ out(有2) → dragger(上拖) → loop_back  ← 拖屏找 1
 │                    └ miss(无1无2) → end                  ← 全清/到底→结束
 └─ ...
```

| 环节 | 现有节点 | 可行性 |
|------|---------|--------|
| 攻击第一个 1 | matcher(index=0) + clicker(识别坐标) | ✅ 已有 |
| 有 1 判定 | matcher out | ✅ |
| 有 2 判定 | matcher out | ✅ |
| 拖屏 | dragger | ✅ |
| 主循环 + 退出 | loop / end | ✅ |
| 拖到底检测 | — | ❌ 缺 |

### 唯一缺口：拖屏到底检测（防死循环）

屏幕只剩 2 时每轮都拖屏 → 到底后画面不变但识别结果仍是「有 2」→ 无限拖。
三个方案：

- **A. 帧对比节点 frame_diff（新增，推荐）**：截图器取两帧（拖前/拖后），
  相似度 ≥ 阈值 → 「拖不动了」→ 结束。通用能力，后续多处可用。
- **B. 拖屏次数上限（零新节点，过渡）**：loop 计数拖 N 次强制结束
  （列表长度有界）。最简单，但 N 依赖任务配置，可能提前/延后结束。
- **C. 图标坐标对比（零新节点，图复杂）**：拖前记录第一个 2 的坐标，
  拖后坐标不变 → 到底。set_var + branch 拼凑可行但繁琐。

### 「触发分支」节点是否需要？

本场景决策是**二分串行判定**（有 1？→ 否 → 有 2？→ 否 → 结束），
两个 matcher 串联即可，**不需要新节点**。
若未来出现「一个信号 → N 个输出」的多路决策（≥3 路），再立项 `switch`
多路分支节点（按变量/信号值路由到对应输出端口）。

另注意：场景信号表（第十一节）当前「取分数最高」在多标志同时命中时不稳定
（有 1 又有 2 时两个场景都命中）——串行判定天然回避该问题；若信号表需支持
此场景，需新增「按顺序取第一个命中」优先级模式（priority_mode: score/order）。

### 可选增强：特征数量识别器

本场景只需存在性，不需计数。若以后要「区域内标志数量」（击杀统计/体力检查），
再落地第五节构想的 matcher_count（输出数量）。

### 结论（已拍板 2026-08-15）

1. 主循环：**现有节点完全可行**（matcher×2 + clicker + dragger + loop）。
2. **新增 `frame_diff` 帧对比节点**（已拍板）：比较拖前/拖后两帧，相似度高→到底。
3. 「触发分支」节点：本场景不加，`switch` 多路分支节点留作未来立项（已拍板）。
4. 素材：icon_1 / icon_2 两个图标素材（示教生成）。

o - o - o - o （主流程）
    |     /    （有个节点有分支）
    o - o          （分支节点）
   / \            
  o - o          （循环部分）

## 十四、进度可视化 + 可调用变量（2026-08-16 拍板）

> 来源：用户口述已拍板 + AI 性能/问题分析。实施前以本节为准。

### 已拍板决策

1. **可调用变量（实时变化模式，2026-08-16 补充拍板）**：
   - 变量组节点数据类型新增「可调用」属性列。
   - 可调用变量 = 实时变化模式（所见即所得），与其他变量无区别，
     只是会被「参数处理」节点改变，且 UI 默认锁编辑。
   - 值跨运行保留（不按日期重置，无累计/失效语义）；按任务独立存储。
2. **参数处理节点语法（2026-08-16 拍板）**：
   - 两个输入：运算符下拉 + 运算值输入。
   - 运算符：`加` `减` `乘` `除以` `取余`（数字类型）；`变化为`（所有类型）；
     `取反`（bool 类型）。
   - 运算值：数字 `1` / `1.001`；带引号文本 `'充分'`（英文 `'` 或 `"` 均可）；
     bool `true` / `false`；`取反` 时运算值可空（null）。
   - 示例：`加 1` = +1；`变化为 3` = 设为 3；`变化为 '你好'` = text 赋值；
     `取反` = bool 取反。类型不匹配报错。
   - UI：可调用变量实时显示；默认只读，点「修改」小图标后输入框才可编辑，
     再点小图标保存同步到任务；运行中实时刷新。
3. **进度节点（2026-08-16 补充拍板）**：
   - 框选多个节点 → 右键「保存为进度节点」= 一个进度组；任务中有几个框选，
     UI 就生成几个 o。
   - o 状态转移：进入组=蓝 / 组内全部走完=绿 / 任一失败=红 / 未执行=灰；
     组内过程不细分。
   - 组间连线：执行游标位于两组之间（未框住的中间节点在执行）时，
     该连线渲染为**由上一组指向下一组的蓝色箭头**；其余为灰线。
   - **循环 = 循环节点 + 循环体整体框选为一个 o**（一个进度组），
     不再显示 n/m 循环进度，直接当一个节点显示。
   - 整图视为线性 + 分支树形（无循环容器嵌套），布局如用户示例
     （主流程横排、分支下挂）。
   - **分支语义（2026-08-16 确认）**：只有实际走过的 o 才变蓝→绿，
     未走到的分支 o 保持灰；分叉时只有当前路径的出边为蓝箭头，
     其余出边灰线。
   - 每次 TASK_STARTED 缩略图重置全灰。

### 性能结论（AI 分析）

- 事件上报 / 缩略图换色（局部 update）/ 每圈 n/m 上报：开销可忽略。
- 蓝色箭头：QGraphicsScene 画线 + 三角箭头，游标切换时局部 update，零成本。
- 可调用变量写盘：内存即时更新 + 节流写盘（1s/次）+ 任务结束/异常强制 flush。
- UI 实时同步：Qt 信号跨线程 + 500ms 节流刷新。

### 待拍板问题

（全部拍板完毕，无遗留。）

### 附带清理项

删除 `_cycle_limit_events` / `cycle_limit_event` / `_on_task_progress` 的
record_cycle 段 / scheduler 的 record_cycle 与 total_count 链路；
task_runtime_progress 进度显示被缩略图取代后退役。

o - o - o - o - o - o - o - o
    |       |       |
    |       o - o   o - o - o
    |
    o - o - o - o - o