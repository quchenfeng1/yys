# 示教节点设计构想（记录 + 可行性分析）

> 记录时间：2026-08-14
> 来源：用户口述，由 AI 整理。本文件为设计讨论底稿，非最终方案。

## 一、用户原始设想（口述要点）

### 1. 示教节点（核心新概念）
- 新增一个「示教节点」，进入示教界面设置好后保存。
- 该节点**向外输出截图的各种处理结果**。
- 当前输出仅两类：**红框**、**蓝框+遮罩**。
- 一个示教节点可能有**多个选框**（多个红框 / 多个蓝框）。

### 2. 基础识图节点的输入
- 所有需要识图的模块，新增两个输入：**特征**、**区域**。
- **特征** = 蓝框内所有遮罩涂出的图标（要识别的东西）。
- **区域** = 红框的范围（在模拟器屏幕的哪个范围去找特征）。

### 3. 五种解耦后的基础节点

| 节点 | 功能 | 输出 |
|------|------|------|
| **识别器** | 判断区域内是否有特征 | true / false |
| **点击器** | 点击区域内的特征图标（可放识别器后，识别到再点） | 成功 true / 没找到 false |
| **OCR 识别器** | 识别目标区域中特征关联文字（蓝框内加**黄框**，识别到特征后去对应黄框位置提取文字） | 提取的文字 |
| **判断器** | 对文字用**正则等高级判断**（不只等于） | 成功 true / 失败 false；可用于循环（后续节点 out 接回前面节点） |
| **特征数量识别器** | 识别目标区域内的特征数量 | 数量 |

### 4. 用户自述
> "我的思路有些乱了，先帮我记录。"

## 二、结构化理解

用户的本质意图：

```
现状：每个识图节点各自配置【模板 + 区域】（参数化，节点间不共享）
目标：示教节点产出【特征 + 区域】数据，识图节点接收（数据流，一处示教多处复用）
```

数据流向（设想）：

```
[示教节点] --特征(蓝框遮罩图标)--> [识别器/点击器/OCR/计数]
           --区域(红框范围)------> [识别器/点击器/OCR/计数]

[识别器] --true/false--> [判断器/循环回跳]
[OCR识别器] --文字--> [判断器(正则)]
```

## 三、与现有体系的映射（可行性判断）

| 用户设想节点 | 对应现有节点 | 差距 |
|-------------|-------------|------|
| 识别器 | `scene_probe` / `matcher`（判断有无） | 语义已具备，差"接收外部特征+区域" |
| 点击器 | `matcher` + `clicker(识别坐标)` | 已具备"识别到再点击中心" |
| OCR 识别器 | `ocr_reader` | 已具备；差"黄框关联区域" |
| 判断器 | `branch` | 差"正则/高级比较"（现有仅 ==/!=/>=/<=/存在） |
| 特征数量识别器 | `matcher`（index 多实例） | 差"返回数量"而非"取第 N 个" |

结论：**不是全新造一套，而是把现有节点拆细 + 用「特征/区域」数据流串起来**。

## 四、待澄清的关键问题（实现前必须拍板）

1. **示教节点形态**：
   - A) 图里的一个节点，输出特征/区域数据（数据流连线）
   - B) 素材库（现有 SceneStore），识图节点下拉引用素材 ID（现状）
   - C) 变量引用（示教输出到变量，识图节点引用变量名）

2. **特征/区域如何传递**：数据端口连线？变量名？素材 ID？

3. **一个示教节点多组特征/区域时**，识别器是"处理全部"还是"指定其中一组"？输出什么？

4. **点击器与识别器的依赖**：控制流顺序（识别器 out → 点击器 in）还是数据流（识别器结果 → 点击器）？

5. **黄框**：蓝框内加黄框 = 特征图标关联的文字区域，需要扩展示教数据结构
   `蓝框 = { 遮罩图标, 黄框(文字区域) }`。

6. **判断器循环**：复用现有 `loop` + `loop_back` 机制，还是判断器自带循环？

## 五、初步可行性结论

**总体可行，方向正确**（从"参数化配置"演进为"数据流传递"，解耦更彻底、复用更强）。

关键设计点：
- 优先复用现有 SceneStore / matcher / ocr_reader / branch，避免推倒重来。
- 「特征 + 区域」建议做成**数据端口连线**（NodeGraphQt 原生支持数据端口）。
- 「判断器」在现有 branch 上增加正则等高级比较即可。
- 「特征数量识别器」= matcher 多实例匹配的计数版。
- 「黄框」是示教数据结构的一次扩展（蓝框内嵌套黄框）。

## 六、截图控制模式（2026-08-14 追加）

用户新增构想：**只有场景判定（识图）节点能控制截图**，防止模拟器无目的截图浪费性能。

搭图约定（状态机模式）：

```
识图1（前置判断：在目标界面）→ 操作（点击/滑动）→ 识图2（结果判断：到达目标界面）
```

- 每个操作节点前后各放一个识图节点。
- 识图2 同时作为「本次操作的结果校验」和「下一个操作的前置判断」。
- 点击未生效 → 识图2 判定失败走 false → 连线回跳识图1 重新判定，形成重试循环。

分析结论：
- ✅ 逻辑清晰、截图次数可控，是好的搭图规范（显式状态机思想）。
- ⚠️ 回跳需注意死循环：现有执行器对普通节点回跳无循环计数（仅 max_steps 兜底），
  建议用 `loop` 节点包裹「识图1→操作→识图2」，或用「计数器 + 判断器」限制重试次数。
- ℹ️ 现状：操作节点（clicker/dragger 等）本就不截图，仅识图类节点（matcher/scene_probe/ocr）截图，
  该约定在代码层面已基本成立，主要作为搭图规范固化。
- 💡 识图1 与识图2 可复用同一特征（特征=目标按钮/页面变化），也可用不同特征。

## 七、失效判断端口设想（2026-08-14 追加）

用户新增构想：给识图节点加「失效判断输入/输出」端口。

```
识图1.失效判断输出 ──> 识图2.失效判断输入
识图2 自身特征失败时 → 转而用识图1 的特征再判断一次：
  - 识图1 特征命中（画面没变，点击未生效）→ 走回点击功能（重试）
  - 识图1 特征也失败（画面既不是1也不是2）→ 异常
```

分析结论：
- ✅ 核心价值：能区分「点击未生效（画面没变）」vs「画面已变但特征没匹配」，对重试判断有用。
- ⚠️ 实现更简单的等价方案：**复用同一特征 + 普通识图节点**，不必新增专用端口。
  `识图2.miss → 识图(按钮还在?) → true 回跳 loop_back / false 走异常`
  其中「按钮还在」与识图1 用同一个特征素材即可。
- 💡 若坚持数据流：本质是「特征从上游传下游」，应做成通用的**特征数据端口**（见第五节），
  而非专用「失效判断」端口——专用端口会让连线在多层借用时越来越乱。

## 八、截图/识图分离 + 信号端口统一（2026-08-14 追加）

用户构想：
1. **截图与识图分离**：同一页面不同状态只需多次判断，不必多次截图。
2. **信号输入/输出**：每个基础节点加「信号输入 + 信号输出」；开始只有信号输出、
   结束只有信号输入；判断节点成功→true 输出信号、失败→false 输出信号。
3. **OCR 节点双输出**：一个信号 + 一个识别值（识别值传入判断节点）。
4. **去掉循环节点**：靠信号回跳成环实现循环（保留「环状节点做循环」思路）。

分析结论：
- ✅ 截图分离是真实优化：建议「截图节点刷新缓存帧 + 识图节点读缓存帧」，
  一次截图多次判断。
- ✅ 信号输入/输出统一，模型更规整（等价于把 out/not_found/miss 统一为 true/false）。
- ⚠️ 矛盾点：「保留环状节点」与「不需要循环节点」二选一。去掉 loop 后，回跳循环
  失去计数/条件保护，死循环只能靠 max_steps 兜底；建议 loop 保留作「有边界循环」，
  信号回跳作「简单重试」快捷方式，二者共存。
- ⚠️ OCR 双输出 = 信号流 + 数据流混合，需要明确「数据端口」机制（识别值如何连线传入
  判断节点），与第五节「特征数据端口」合并设计。

## 九、状态转移验证节点（2026-08-14 追加）

用户洞察：识图本质是「两张图的对比」= 两个特征的状态转移：

```
特征1 识别 → 操作 → 识别特征2
  ├ 特征2 识别到 → 成功(out)
  └ 特征2 未识别 → 回判特征1
        ├ 特征1 还在 → 再操作（retry，重试）
        └ 两者都没识别 → 异常(unknown，走示教/报错)
```

封装为单个基础节点（建议名：状态转移 / 操作验证）。

参数：特征1、特征2、操作（点击点/滑动）、重试次数。
端口：in / out(到达特征2) / retry(还在特征1) / unknown(两者皆无，接示教)。

结论：✅ 正确的抽象，覆盖游戏自动化最高频的「点击 → 验证跳转」模式；
封装后一个节点替代「识图1+操作+识图2+回判识图1+重试」整条链。

## 十、场景状态机架构（2026-08-14 追加，重大思路转变）

用户重新梳理：**脚本不是一条线，而是「场景状态机」**。

- 每个任务有若干固定画面（场景）；只要还在这几个场景中 = 未脱离任务。
- 从每个场景出发操作，操作后识图，识别出哪个场景就跳转到该场景继续下一步。
- 每个场景的操作/判断逻辑完全分开。
- 操作后还在原场景 → 重试；连续识别出 N 次同一场景 → 报错。
- 识图结果不在任何已知场景 → 异常（脱离任务，接示教）。

结论：✅ 这是游戏自动化最成熟的「有限状态机 + 场景识别」架构，方向正确。

落地关键（待实现）：
- 「多场景识别」：一次截图匹配所有场景，返回命中场景 id（现有 scene_probe 是单场景判定，需扩展或新增）。
- 异常 = 不在任何已知场景 → 接示教（on_unknown）。
- 重试上限 = 连续 N 次识别出同一场景 → 报错/异常处理。

## 十一、场景状态机·最终设计定稿（2026-08-14）

> 用户已把思路捋清，以下为完整架构。**换设备续接时以本节为准。**

### 核心思想
脚本不是一条线，而是「有限状态机」：每个任务有若干固定画面（场景），
只要还在这些场景中 = 未脱离任务。从每个场景出发操作，操作后识图，
识别出哪个场景就跳转到该场景继续。

### 架构分层

1. **中央识别层：场景信号表**
   - 一个「区域特征 → 场景信号」的映射表（动态列表，场景数量可变）。
   - 一次截图匹配所有特征，命中哪个特征就输出对应场景信号（推到外部）。
   - 与单场景识别（scene_probe，输出 true/false）的区别：信号表做多分类，输出场景 id。
   - 全无命中 → 异常信号（脱离任务）。

2. **场景任务层：场景起始节点 + 场景内操作**
   - 每个场景一个「场景起始节点」，声明监听自己的场景信号；信号对上则激活该场景任务。
   - 场景内操作：大部分是单点击（点完进入其他画面）；也有「点击后判断开关是否打开」、
   或 OCR 识别出体力不足走不同操作（场景内分支）。

3. **跳转优化：期望页面快速路径**
   - 跳转一般是固定的 → 操作后先查「期望页面」（单场景识别，开销小），命中直接固定跳转。
   - 只有没识别到期望页面时，才回查全局信号表：判断仍在任务内哪个场景 / 还是跳出任务异常。

### 数据流（示意）

```
[场景信号表] --场景信号--> 激活对应[场景起始节点]
[场景起始节点] --操作--> [期望页面检查(scene_probe)]
  ├ 命中期望 --> 固定跳转到下一场景起始
  └ 未命中 --> 回查[场景信号表]
       ├ 命中某场景 --> 激活该场景（异常跳转但仍在任务内）
       └ 全无命中 --> 异常(跳出任务) --> 接示教
```

### 落地映射（复用 vs 新增）

| 概念 | 落地方案 |
|------|---------|
| 场景信号表 | **新增**：多场景识别器（复用 SceneStore + _match_template） |
| 信号分发/激活 | **复用** event_bus（信号名匹配激活场景起始节点） |
| 场景起始节点 | **新增** scene_entry（声明监听场景 id） |
| 期望页面检查 | **复用** scene_probe（单场景判定） |
| 场景内判断 | **复用** ocr_reader + branch/checker |
| 单点击操作 | **复用** clicker |

### 待拍板的 3 个细节（下次会话继续）
1. 场景信号表结构：一个场景挂一个还是多个特征？（建议：可挂多个，任一命中即判定该场景）
2. 多场景同时命中优先级：（建议：信号表顺序取第一个，或取匹配分数最高）
3. 异常判定语义：未命中期望 → 回查全局 → 命中其他场景=仍在任务内跳转；全无命中=异常接示教。

### 之前讨论中已收敛的结论（备查）
- 截图与识图分离：截图节点刷新缓存帧，识图节点读缓存帧（一次截图多次判断）。
- 信号端口统一：节点 = 信号输入 + 成功/失败信号输出（等价于统一 out/not_found/miss）。
- loop 节点保留：作「有边界的循环」（战斗次数等大循环）；「点击失败小回调」用
  识图节点内置 retry 端口 + 重试次数参数，不用 loop。
- 状态转移验证节点（特征1→操作→特征2）已被「场景状态机」架构吸收，不再单独立项。
