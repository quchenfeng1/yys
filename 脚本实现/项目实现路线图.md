# 项目实现路线图 v1.0

> **文档性质**：分阶段施工计划，每阶段明确目标、任务、验证标准  ｜  **配套**：全部 `脚本实现/` + `模块说明/` 文档

---

## 总览

```
Phase 0  ✅ 基础底板     01设备 + 02识图 + 03防封 + 06配置 + 08事件 + 12日志 + 14执行器
Phase 1  ✅ 任务引擎     04-任务执行引擎 + 9个通用模块
Phase 2  ✅ 调度运行时    05-时间调度模块 + 07-运行时状态管理
Phase 3  ✅ UI + 运行     09-运行控制中心 + 11-用户界面模块 + 13-任务文件管理 + 16-应用启动引导
Phase 4  🔵 当前          uses_* UI联动 + 阵容/御魂模块 + OCR体力检查
Phase 5  ⬜ 账号多开      15-账号管理模块 + 多模拟器切换
Phase 6  ⬜ 参数桥接      10-参数桥接模块 (UI↔逻辑唯一通道)
Phase 7  ⬜ 补齐收尾      补全占位面板 + 端到端测试 + 文档终审
```

---

## Phase 0：基础底板 ✅ 已完成

> **目标**：能截图、能识图、能点击、能配参数、能记日志

| 任务 | 涉及文件 | 状态 |
|------|---------|:--:|
| ADB 连接/截图/点击/滑动/启动App | `device/adb_client.py` | ✅ |
| 模拟器管理（启动/端口检测/保活） | `device/emulator.py` | ✅ |
| 连接池管理 + 心跳检测 | `device/connection.py` | ✅ |
| 模板匹配（灰度+缓存+区域限定） | `core/recognizer.py` | ✅ |
| 防封号随机参数生成 | `core/anti_detect.py` | ✅ |
| YAML/JSON 配置加载 + 点分路径访问 | `core/config_manager.py` | ✅ |
| 配置校验 | `core/config_schema.py` | ✅ |
| 发布-订阅事件总线（线程安全） | `core/event_bus.py` + `core/events.py` | ✅ |
| loguru 结构化日志 | `core/logger.py` | ✅ |
| 操作执行桥接（识图→偏移→点击） | `core/executor.py` | ✅ |

**验证**：
```
□ ADB 连接成功 → 能截图 → 图像非空
□ 放一张素材到 assets/ → Recognizer.find() 返回 MatchResult
□ Executor.click_image() 成功执行点击
□ global.yaml 配置可读，修改后生效
□ EventBus.publish/subscribe 正常工作
□ 日志文件生成，格式正确
```

---

## Phase 1：任务引擎 ✅ 已完成

> **目标**：任务能拆成步骤、能用图编排、有通用模块复用

| 任务 | 涉及文件 | 状态 |
|------|---------|:--:|
| TaskStep 基类 + StepResult | `tasks/base/task_step.py` | ✅ |
| TaskGraph 有向图引擎 | `tasks/base/task_graph.py` | ✅ |
| TaskContext 步骤间上下文 | `tasks/base/task_context.py` | ✅ |
| ClosePopup 关弹窗 | `tasks/common/close_popup.py` | ✅ |
| ReturnHall 返回庭院 | `tasks/common/return_hall.py` | ✅ |
| ErrorReturn 错误恢复 | `tasks/common/error_return.py` | ✅ |
| BattleLoop 战斗循环 | `tasks/common/battle_loop.py` | ✅ |
| ClaimReward 领取奖励 | `tasks/common/claim_reward.py` | ✅ |
| OpenBottomMenu 主界面识别 | `tasks/common/open_bottom_menu.py` | ✅ |
| 其余 3 个通用模块 | `select_team / pre_check_team / check_stamina` | ⚠️ 依赖未实现 |

**Phase 1 遗留**：`SelectTeam` / `PreCheckTeam` / `CheckStamina` 三个通用模块内部依赖 `team_manager` 或 OCR，当前为骨架/占位。

**验证**：
```
□ TaskGraph 添加步骤 → 连线 → run() → 按序执行
□ ClosePopup + ReturnHall 组合能跑通
□ BattleLoop(times=3) 循环 3 次后返回 success
□ 任意步骤 fail → 自动走 error 分支
```

---

## Phase 2：调度 + 运行时 ✅ 已完成

> **目标**：任务能按时间调度、状态能跨模块共享

| 任务 | 涉及文件 | 状态 |
|------|---------|:--:|
| RepeatRule 8 种执行规则 | `core/repeat_rule.py` | ✅ |
| TaskStateStore 原子持久化 | `core/task_state.py` | ✅ |
| Scheduler 日程表引擎 | `core/scheduler.py` | ✅ |
| StateManager 运行时状态 | `core/state_manager.py` | ✅ |
| StateKeys 状态键常量 | `core/state_schema.py` | ✅ |

**验证**：
```
□ Scheduler.load_tasks_from_config() 从 tasks.yaml 加载
□ build_schedule() 返回到期任务列表（按 priority 排序）
□ mark_done(success=True) → next_run_time 推进
□ mark_done(success=False) → fail_streak 递增 + 冷却
□ task_state.json 原子写盘 → 重启后恢复
□ StateManager.set_state() → 发布 STATE_CHANGED 事件
```

---

## Phase 3：UI + 运行 ✅ 已完成

> **目标**：有 GUI、能启动脚本、能持续调度执行

| 任务 | 涉及文件 | 状态 |
|------|---------|:--:|
| MainWindow 三栏布局 | `ui/main_window.py` | ✅ |
| MenuTree 7 组菜单路由 | `ui/panels/menu_tree.py` | ✅ |
| ControlBar 启停按钮状态机 | `ui/panels/control_bar.py` | ✅ |
| TaskQueuePanel 队列卡片 | `ui/panels/task_queue_panel.py` | ✅ |
| LogPanel 日志+内置终端 | `ui/panels/log_panel.py` | ✅ |
| StatusBar 底部状态栏 | `ui/panels/status_bar.py` | ✅ |
| ConfigPanel 内联配置 | `ui/panels/config_panel.py` | ✅ |
| ImageManagerPanel 素材管理 | `ui/panels/image_manager_panel.py` | ✅ |
| TaskManagerPanel 任务CRUD | `ui/panels/task_manager_panel.py` | ✅ |
| 游戏任务配置表单 | `main_window.py` 内联 | ✅ |
| ScriptWorker QThread 8步初始化 | `ui/script_worker.py` | ✅ |
| _run_schedule_loop 持续调度 | `ui/script_worker.py` | ✅ |
| TaskManager 扫描/创建/删除/解析 | `core/task_manager.py` | ✅ |
| LoginFlow 登录流程 | `tasks/login_flow.py` | ✅ |

**验证**：
```
□ GUI 启动 → 7 组菜单正常显示
□ 菜单点击 → 中间面板正确切换
□ 全局控制视图 → 任务队列卡片正常
□ 点击启动 → ScriptWorker 初始化 → 连接模拟器 → 登录
□ 登录成功 → 调度循环开始 → 任务到期自动执行
□ 点击停止 → 调度循环退出 → 状态保存
□ 日志终端实时显示、无乱码
```

---

## Phase 4：补齐关键功能 🔵 当前阶段

> **目标**：uses_* UI 联动、阵容/御魂通用模块、OCR 体力检查

### 4.1 uses_* UI 动态配置区

| 子任务 | 涉及文件 | 优先级 |
|--------|---------|:--:|
| `_on_game_task_clicked()` 读取 `task_module.uses_*` 字段 | `ui/main_window.py` | 🔴 |
| 根据 `uses_battle` 显示阵容/御魂/失败容忍配置区 | `ui/main_window.py` | 🔴 |
| 根据 `uses_team` 显示 team_id 下拉框 | `ui/main_window.py` | 🔴 |
| 根据 `uses_soul` 显示 soul_setup 配置编辑区 | `ui/main_window.py` | 🔴 |
| 根据 `uses_stamina` 显示 stamina_required 输入 | `ui/main_window.py` | 🔴 |
| 根据 `loop_count` 显示每轮循环次数输入 | `ui/main_window.py` | 🟡 |

### 4.2 阵容/御魂支持

| 子任务 | 涉及文件 | 优先级 |
|--------|---------|:--:|
| TeamManager 类（管理阵容预设配置） | `core/team_manager.py` (新建) | 🟡 |
| 完善 SelectTeam：调用 TeamManager | `tasks/common/select_team.py` | 🟡 |
| 完善 PreCheckTeam：调用 TeamManager | `tasks/common/pre_check_team.py` | 🟡 |
| StartBattle 通用模块 | `tasks/common/start_battle.py` (新建) | 🟡 |

### 4.3 OCR 体力检查

| 子任务 | 涉及文件 | 优先级 |
|--------|---------|:--:|
| 完善 TextLocator（pytesseract 集成） | `core/ocr_locator.py` | 🟢 |
| 完善 CheckStamina：OCR 读取体力值 | `tasks/common/check_stamina.py` | 🟢 |

**Phase 4 验证**：
```
□ 新建 battle 类型任务 → _on_game_task_clicked 显示阵容配置区
□ 新建 event_task 类型任务 → 不显示阵容配置区
□ 修改 uses_team=True → UI 显示 team_id 下拉框
□ SelectTeam.execute() 能选择阵容（无异常）
□ StartBattle.execute() 能点击挑战按钮
□ CheckStamina.execute() 能 OCR 读取体力并判断
```

---

## Phase 5：账号多开 ⬜

> **目标**：支持主号+小号、多模拟器切换

| 任务 | 涉及文件 | 优先级 |
|------|---------|:--:|
| AccountManager 类实现 | `core/account_manager.py` (新建) | 🔴 |
| load_accounts() 从 accounts.yaml 加载 | 同上 | 🔴 |
| switch_to(account_id) 切换账号 | 同上 | 🔴 |
| get_task_scope() 按角色返回任务范围 | 同上 | 🔴 |
| ScriptWorker 集成账号切换逻辑 | `ui/script_worker.py` | 🔴 |
| UI 小号设置面板实现 | `ui/panels/sub_account_panel.py` (新建) | 🟡 |
| 状态栏实时显示当前账号 | `ui/panels/status_bar.py` | 🟡 |

**Phase 5 验证**：
```
□ AccountManager 从 accounts.yaml 加载 1 主号 + 2 小号
□ switch_to("sub1") → ADB 断开主号 → 连接小号模拟器
□ 状态栏从「👤 主号」变为「👤 小号1」
□ 主号执行全部任务，小号只执行 permanent 分类
□ 小号任务完成后自动切回主号
```

---

## Phase 6：参数桥接 ⬜

> **目标**：UI 通过 ParamBridge 统一访问逻辑层，不再直接持有模块引用

| 任务 | 涉及文件 | 优先级 |
|------|---------|:--:|
| ParamBridge 主类 | `core/param_bridge.py` (新建) | 🔴 |
| RunBridge: 启停信号桥接 | 同上 | 🔴 |
| TaskBridge: 任务配置桥接 | 同上 | 🔴 |
| ConfigBridge: 配置读写桥接 | 同上 | 🔴 |
| UIBinding: 控件↔配置双向绑定 | 同上 | 🟡 |
| MainWindow 重构：改为通过 ParamBridge 访问逻辑 | `ui/main_window.py` | 🟡 |
| 各 UI 面板重构：通过 bridge 而非直接 import | 各 panel 文件 | 🟡 |

**Phase 6 验证**：
```
□ UI 点击启动 → ParamBridge → EventBus.publish(start_requested) → ScriptWorker 响应
□ UI 修改配置 → ParamBridge → ConfigManager.set() → 原子写盘
□ MainWindow 不再直接 import Scheduler / ConfigManager / TaskManager
```

---

## Phase 7：补齐收尾 ⬜

> **目标**：补全占位面板、端到端测试、文档终审

| 任务 | 涉及文件 | 优先级 |
|------|---------|:--:|
| 运行监控面板实现（指标/截图/报告） | `ui/panels/monitor_panel.py` (新建) | 🟢 |
| 账号管理配置面板实现 | `ui/panels/config_panel.py` | 🟢 |
| 阵容预设配置面板实现 | `ui/panels/config_panel.py` | 🟢 |
| 端到端测试：从零创建任务 → 跑通全流程 | 按 `端到端实战指南.md` 执行 | 🟢 |
| 异常场景测试：断连重连、素材缺失、超时 | 按 `异常处理与容错设计.md` 验证 | 🟢 |
| 文档终审：模块说明 + 脚本实现 全量交叉验证 | 全部文档 | 🟢 |

**Phase 7 验证**：
```
□ 运行监控面板显示真实指标数据
□ 所有占位面板替换为实际功能
□ 按端到端实战指南创建任务 → 跑通无 ERROR
□ 模拟断连 → 状态栏变红 → 重新连接恢复
□ 全部文档交叉验证无矛盾
```

---

## 当前进度总结

| 阶段 | 状态 | 完成度 |
|------|:--:|:--:|
| Phase 0 基础底板 | ✅ | 100% |
| Phase 1 任务引擎 | ✅ | 85%（3 个通用模块骨架化） |
| Phase 2 调度运行时 | ✅ | 100% |
| Phase 3 UI + 运行 | ✅ | 90%（部分面板占位） |
| **Phase 4 补齐关键** | 🔵 | **0%** |
| Phase 5 账号多开 | ⬜ | 0% |
| Phase 6 参数桥接 | ⬜ | 0% |
| Phase 7 补齐收尾 | ⬜ | 0% |

---

## 下一步行动

**Phase 4 任务 4.1**（预估 1 会话）：
1. `main_window.py` `_on_game_task_clicked()` → 读取 `task_module.uses_*`
2. 根据 `uses_battle` / `uses_team` / `uses_soul` / `uses_stamina` 动态 show/hide 配置区
3. 保存时将这些字段写入 `tasks.yaml`
