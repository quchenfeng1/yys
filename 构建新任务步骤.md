# 构建新任务步骤（从 UI 入门 · 识图脚本）

> **目标读者**：不了解脚本内部构造的人。
> **一句话**：建文件、建目录、写配置——这些 UI 都已帮你自动完成。你只需要做 5 件事：
> ① 在 UI 里「新建任务」→ ② 把游戏截图放进「素材管理」→ ③ 打开编辑，在骨架里填"识别什么图、点哪里" → ④ 在 UI 里配置时间并保存 → ⑤ 点「启动」。
> 全程唯一要写代码的是第 ③ 步（约 10 分钟），而且模板已给好格式，照着填即可。

---

## 〇、一个识图任务由三部分组成

| 部分 | 是什么 | 放在哪 | 谁负责 |
|------|--------|--------|--------|
| 素材（图片） | 识图的眼睛：按钮截图、界面截图 | `assets/` 目录 | 你在 UI「素材管理」添加 |
| 任务代码（.py） | 识别+点击的逻辑：看到什么图 → 点什么 | `tasks/分类/任务名.py` | UI 生成骨架，你填逻辑 |
| 配置（tasks.yaml） | 什么时候跑：时间/优先级/次数 | `config/tasks.yaml` | UI 自动写 + 你在表单里改 |

**识图脚本的本质**：循环做两件事——`识别图片`（用截图匹配屏幕上画面）→ `点击位置`（匹配到就点它）。

---

## 一、启动 UI 并认识界面

**启动**：双击 `新程序/主程序/启动UI.command`（macOS），或在该目录执行 `./启动UI.command`。

**界面三栏**：
- 左栏：菜单树（8 项：游戏任务 / 任务队列 / 任务管理 / 配置 / 素材管理 / 小号管理 / 执行历史 / UI 设置）
- 中栏：当前面板内容
- 右栏：日志 + 终端（可点左上角按钮收起）

**构建任务用到的 3 个面板**：
| 面板 | 用途 |
|------|------|
| 📁 任务管理 | 新建任务（生成骨架）、打开编辑、删除 |
| 🖼 素材管理 | 放截图、看引用路径 |
| 📋 游戏任务 | 配置运行时间/优先级并保存 |

## 二、用 UI 新建任务（1 分钟）

**操作路径**：左栏「📁 任务管理」→ 点「新建」→ 依次填：

1. **任务类型**：识图脚本（点击+识别）选 **非战斗任务**；玩副本战斗选 **战斗任务**（本文以非战斗为例）
2. **任务文件名**：英文小写，如 `my_first_task`（不要 `.py`）
3. **显示名**：中文，如 `我的第一个识图任务`
4. **分类**：`daily`（日常）/ `permanent`（常驻）/ `event`（活动）/ `special`（特殊），随便选一个

点确定后，**UI 自动完成 3 件事**（不用你手动做）：
- 生成任务骨架文件 `tasks/{分类}/my_first_task.py`（含注释指引）
- 在 `config/tasks.yaml` 追加默认调度条目（默认每天执行）
- 创建任务专属图片文件夹 `assets/tasks/my_first_task/`

> 校验：任务列表里应出现该任务，右侧详情能看到"任务类型: 非战斗任务"。

---

## 三、用 UI 放素材（截图）

素材 = 任务的眼睛。**图片放哪，代码就引用哪**，命名随意但要能看懂。

**操作路径**：左栏「🖼 素材管理」→ 左侧「图片位置」选你的任务文件夹（`📁 我的第一个识图任务`）→ 点「➕ 添加图片」→ 多选 PNG 截图。

**引用路径规则**（在素材管理右侧预览区能看到"引用路径"）：
| 图片位置 | 代码里的引用路径 |
|---------|----------------|
| 任务专属 `assets/tasks/my_first_task/` | `tasks/my_first_task/图片名`（去 `.png`）|
| 通用共享 `assets/tasks/_shared/` | `tasks/_shared/图片名` |
| 识图场景 `assets/scene/` | `scene/图片名`（场景感知用）|

> 例：把 `挑战按钮.png` 放进任务文件夹 → 代码写 `"tasks/my_first_task/挑战按钮"`。
> 建议分辨率与模拟器一致（默认 1280×720），图片只截按钮本身、背景干净些。

---

## 四、打开编辑，写识别逻辑（唯一要写代码的一步）

**操作路径**：任务管理 → 选中你的任务 → 点「打开编辑」（用 VSCode 打开骨架）。

骨架里已经有：模块声明 + 一个示例步骤 `StepOne` + 入口类。你只需要改两处：

### 4.1 改示例步骤 `StepOne.execute()` —— 写"识别什么图、点什么"

把 TODO 替换成真实逻辑。**点击一张图**用 `click_image`：

```python
class StepOne(TaskStep):
    """步骤1 点击目标按钮"""
    is_generic = False
    timeout = 20

    def execute(self, context=None):
        exe = context.executor
        ok = exe.click_image(
            "tasks/my_first_task/挑战按钮",      # ★ 引用路径（见素材管理）
            timeout=5,
            stop_event=getattr(context, 'stop_event', None),
        )
        if not ok:
            return StepResult.fail("未找到挑战按钮")
        return StepResult.success("已点击挑战按钮")
```

**点击 + 等待界面**用 `click_image` + `detect_scene`（识别到才继续，否则失败）：

```python
    def execute(self, context=None):
        exe = context.executor
        if not exe.click_image("tasks/my_first_task/挑战按钮", timeout=5,
                               stop_event=getattr(context, 'stop_event', None)):
            return StepResult.fail("未找到挑战按钮")
        # 识别到"战斗界面"才继续（素材放 assets/scene/，引用 scene/战斗界面）
        scene = exe.detect_scene(["scene/战斗界面"], timeout=5)
        if not scene:
            return StepResult.fail("未进入战斗界面")
        return StepResult.success("已进入战斗界面")
```

**只会用这 3 个执行器方法就够了**：
| 方法 | 作用 |
|------|------|
| `exe.click_image(路径, timeout=...)` | 识别到图就点击，返回是否成功 |
| `exe.detect_scene([...], timeout=...)` | 识别其中任一图，返回场景名或 None |
| `exe.click_if_exists(路径, timeout=...)` | 有就点，没有也继续（可选）|

### 4.2 改入口类 `_build_graph()` —— 把步骤连起来

骨架入口类里 `_build_graph()` 已经初始化了 `TaskGraph`，把示例改成你的步骤并连线：

```python
class MyFirstTask(BaseTask):
    """我的第一个识图任务"""
    task_id = "my_first_task"      # ★ 必须与 tasks.yaml 的 name 一致（UI 已写对，别改）
    category = "special"           # 与新建时选的分类一致

    def _build_graph(self):
        graph = TaskGraph()
        graph.add_step("step1", StepOne())
        graph.set_entry("step1")          # 入口
        # 更多步骤就继续：
        # graph.add_step("step2", StepTwo())
        # graph.add_edge("step1", "step2")
        return graph
```

保存文件，任务就写好了。

> 想多做几步？照 `StepOne` 复制一个类改逻辑，在 `_build_graph` 里 `add_step` + `add_edge` 串起来即可（一步做完 → 下一步）。

---

## 五、用 UI 配置并保存

**操作路径**：左栏「📋 游戏任务」→ 左侧选你的任务 → 右侧表单：
- ☑ **启用此任务**（必须勾选）
- **优先级**：数字越小越先执行
- **时间调度**：重复规则（每天/每周几/间隔N天…）、执行时段
- **每日上限**：0 = 不限

点「💾 保存配置」，状态栏提示 `已保存 · 下次执行: ...`。

> 配置自动写回 `config/tasks.yaml`（就是新建时 UI 生成的那条，运行期调度模块读取）。

---

## 六、运行验证

1. 顶部控制栏点「🔍 自检」→ 弹窗确认 ADB/素材/配置/依赖是否 OK（无真实模拟器可先开「配置 → 设备 → 模拟设备模式」）
2. 勾选「🧪 沙盒」开关 → 点「▶ 启动」——**沙盒模式只走流程不真点击**，适合第一次试跑
3. 看右栏日志：`设备连接成功 → 运行已启动 → 任务已入队: my_first_task → 步骤完成`
4. 确认流程无误后，取消「🧪 沙盒」再启动，正式运行

> 任务不到执行时段怎么办？在「游戏任务」表单把执行时段设成覆盖当前时间，或把重复规则临时改成 `on_enter`（每次启动执行一次）。

---

## 七、进阶玩法（可选）

- **战斗任务**：新建时选「战斗任务」，UI 自动出现「战斗配置」Tab（御魂/锁定/换队），配合 `tasks/common/` 的通用模块（`BattleLoop`/`CheckStamina`/`ClaimReward`/`ReturnHall`）自动循环刷本。
- **识图触发任务**：新建时选「触发任务」，配置表单填「触发模板」（识图列表），TriggerWatcher 识别到就自动执行；也可在「任务队列」面板点「⚡触发」手动触发。
- **场景感知**：步骤加 `scene_probe = ["scene/主界面"]`，执行前先静默确认当前位置再动作。

---

## 八、完整代码模板（非战斗识图任务，可直接复制）

```python
"""我的第一个识图任务"""
display_name = "我的第一个识图任务"
description = "示例：识别挑战按钮并点击，确认进入战斗界面"
task_type = "event_task"        # "battle" 战斗任务 / "event_task" 非战斗
uses_battle = False
uses_team = False
uses_soul = False
uses_stamina = False
loop_count = 1
timeout = 300

from tasks.base.base_task import BaseTask
from tasks.base.task_graph import TaskGraph
from tasks.base.task_step import StepResult, TaskStep


class StepOne(TaskStep):
    """步骤1 点击挑战按钮"""
    is_generic = False
    timeout = 20

    def execute(self, context=None):
        exe = context.executor
        if not exe.click_image("tasks/my_first_task/挑战按钮", timeout=5,
                               stop_event=getattr(context, 'stop_event', None)):
            return StepResult.fail("未找到挑战按钮")
        scene = exe.detect_scene(["scene/战斗界面"], timeout=5)
        if not scene:
            return StepResult.fail("未进入战斗界面")
        return StepResult.success("已进入战斗界面")


class MyFirstTask(BaseTask):
    """我的第一个识图任务"""
    task_id = "my_first_task"    # 与 tasks.yaml 的 name 一致
    display_name = "我的第一个识图任务"
    description = "示例：识别挑战按钮并点击"
    category = "special"

    def _build_graph(self):
        graph = TaskGraph()
        graph.add_step("step1", StepOne())
        graph.set_entry("step1")
        return graph
```

> 直接复制时把 `my_first_task`、`挑战按钮`、`scene/战斗界面` 换成你自己的名字/图片路径即可。

---

## 九、检查清单（做完任务后逐项核对）

**已在 UI 完成**
- [ ] 任务管理 → 新建 → 任务出现在列表（任务类型正确）
- [ ] 素材管理 → 图片已放进任务文件夹，右侧能看到引用路径
- [ ] 游戏任务 → 表单已保存（enabled 勾选、时间配置正确）

**代码里确认**
- [ ] `StepOne.execute()` 的 `click_image` 路径 = 素材管理显示的引用路径
- [ ] 入口类 `task_id` 与 `tasks.yaml` 的 `name` 一致（UI 新建时已对齐，别改）
- [ ] `_build_graph()` 里 `set_entry` 指向第一步，步骤按顺序 `add_edge` 串联

**运行验证**
- [ ] 自检弹窗通过
- [ ] 沙盒模式启动 → 日志出现 `任务已入队: 你的任务名` → 步骤执行日志
- [ ] 关闭沙盒正式启动 → 识别/点击真实生效

---

## 十、常见问题

| 问题 | 解决 |
|------|------|
| 任务没出现在 UI 列表 | 文件名不能用中文；文件必须在 `tasks/daily\|event\|permanent\|special/` 下；重启 UI 自动重新扫描 |
| 点启动没入队 | 「游戏任务」里确认已保存配置、`enabled` 勾选、当前时间在 `time_start~time_end` 内 |
| 日志报"素材缺失" | 素材管理里确认图片文件名与代码引用**完全一致**（含 `tasks/任务名/` 前缀、去 `.png`）|
| 识别总不中 | 截图分辨率与模拟器一致（默认 1280×720）；只截按钮本身、背景干净 |
| 想删除/改名任务 | 任务管理 → 选中 → 删除（重命名 `.deleted` 可恢复）|
| 想每次启动都执行 | 游戏任务表单把重复规则改成 `on_enter` |

